---
milestone: v1.2
audited: 2026-05-09T23:55:00.000Z
status: gaps_found
scores:
  requirements: 16/18
  phases: 1/1
  integration: n/a (single-phase milestone)
  flows: human_needed (3 UI flows deferred)
gaps:
  requirements:
    - id: "UI-01"
      status: "partial"
      phase: "11"
      claimed_by_plans: ["11e"]
      completed_by_plans: ["11e"]
      verification_status: "human_needed"
      evidence: "Detail.tsx compiles + pnpm tsc clean; useGameStaff/useGameOfficialTags hooks added; ExtSourcePill renders Bangumi/VNDB jump links. Real-app browser verification of paragraph rendering, staff chip layout, and external-link IPC observation deferred."
    - id: "UI-02"
      status: "partial"
      phase: "11"
      claimed_by_plans: ["11e", "11g"]
      completed_by_plans: ["11e", "11g"]
      verification_status: "human_needed"
      evidence: "Person chip click → useNavigate('/persons/:id') wired; /persons/:id route registered in router.tsx (11g f48f86c). 官方标签 region renders below TagPicker. Real-app navigation transition + popover layout deferred."
    - id: "UI-03"
      status: "partial"
      phase: "11"
      claimed_by_plans: ["11f"]
      completed_by_plans: ["11f"]
      verification_status: "human_needed"
      evidence: "FilterPanel.tsx adds 4 new sections (品牌/编剧/画师/声优/官方标签); Library.tsx fetches getFilterOptions() at mount + scan-completion edge; refetchGrid merges new SearchFilter fields into search_games invoke. Real-app multi-axis filter behavior + 更多 expander UX deferred."
  integration: []
  flows:
    - flow: "scan a new game with bound Bangumi id → see staff/tags/summary in Detail"
      status: "compiled, untested in real app"
      reason: "Backend write path verified via cargo test (write_staff_and_tags helper). End-to-end flow needs running app + scanning a real galgame directory."
    - flow: "click brand chip on Library facet → grid narrows to that brand"
      status: "compiled, untested"
      reason: "applyAdvancedFilter handles brand client-side; backend search_games accepts brands[]. Real-network behavior with populated facet options deferred."
    - flow: "backfill_metadata_enrichment IPC against an existing 200-game library"
      status: "compiled, untested"
      reason: "IPC registered + emits meta-fetch-progress per game; Bangumi 1req/s × 3 endpoints/game → ~10 min for 200 games. Real-app run with progress-bar observation deferred."
tech_debt:
  - phase: "11"
    items:
      - "Person dedup across Bangumi+VNDB sources (same author appears as 2 rows). Carried to v1.3 seed."
      - "Person aggregate page enrichment (作品时光轴 / 同台伙伴推荐) — seeds/persons-page-enrichment.md."
      - "Person portrait local caching not implemented (人物聚合页 currently shows no avatar)."
      - "Backfill progress UI minimal — emits events but no dedicated progress bar in PageHeader. Currently piggybacks on existing meta-fetch-progress channel."
      - "tauri-plugin-opener not added; uses cmd /C start fallback. Acceptable per Windows-only constraint but loses cross-platform openness."
      - "Brand filter is double-applied (server-side + client-side in applyAdvancedFilter) — intentional for visible-row count consistency, but slight redundancy in code."
nyquist:
  scope: "skipped (workflow.nyquist_validation == false in config)"
---

# Milestone v1.2 — Metadata Enrichment & Filtering — Audit

**Audited:** 2026-05-09
**Status:** gaps_found (3 UI requirements `human_needed` per autonomous-mode policy — defer to real-app verification)

## Score

| Dimension | Score | Notes |
|-----------|-------|-------|
| Requirements | 16/18 | 15 backend reqs auto-verified via cargo test + pnpm tsc; 3 UI reqs (UI-01/02/03) compile but await real-app smoke test |
| Phases | 1/1 | Phase 11 SUMMARY.md + VERIFICATION.md both present |
| Integration | n/a | Single-phase milestone; Phase 11's commands.rs/lib.rs wire-up verified by `cargo build --lib` succeeding |
| End-to-end flows | 3 deferred | scan→Detail / facet→narrow grid / backfill 200-game library — all need real-app observation |

## Requirements coverage

### MET (Schema) — 5/5 satisfied

All 5 requirements (MET-01..05) auto-verified by `migrations_v7_adds_metadata_enrichment` test (asserts ALTER TABLE games ADD COLUMN summary, 3 new tables, role CHECK constraint, ON DELETE CASCADE, 4 indexes, schema_version='7').

### API (Client widening) — 5/5 satisfied

- API-01: Bangumi `fetch_detail` returns brand + tags — `extract_brand_*` tests pass
- API-02: `bangumi::fetch_persons` + role normalization — `role_normalization_*` test passes
- API-03: `bangumi::fetch_characters` voice + character_name — function compiles + cargo test passes
- API-04: VNDB GraphQL widened with staff/va/developers/tags — DetailResp/StaffResp/VaResp Deserialize types verified by build
- API-05: Cross-source role normalization (incl. art+chardesign collapse) — `role_normalization_collapses_art_and_chardesign` test passes

### ING (Ingest pipeline) — 4/4 satisfied

- ING-01: Ingest writes summary + brand (COALESCE preserves manual) — verified in commands.rs `apply_ingest_result` UPDATE statement
- ING-02: persons / game_staff written via `write_staff_and_tags` — tx-wrapped helper called from all 4 ingest sites
- ING-03: game_official_tags written by same helper
- ING-04: `backfill_metadata_enrichment` IPC — registered in lib.rs invoke_handler

### UI (Frontend) — 2/4 satisfied + 2 partial

- UI-01: Detail page summary / staff / external links — **partial (compiled, awaits real-app)**
- UI-02: Person chip click → /persons/:id; official tags region — **partial (compiled, awaits real-app)**
- UI-03: Library FilterPanel multi-dim facets — **partial (compiled, awaits real-app)**
- UI-04: `/persons/:id` route registered + Persons.tsx renders 4 role-grouped grids — satisfied (route registration + GameCard reuse verified by `pnpm tsc --noEmit` clean)

Wait — recount: UI-04 is the `/persons/:id` aggregate page; that's verified at the type/routing level. UI-01/02/03 each have a code path that compiles + types check, but the rendering and interaction-level outcomes need a running browser. The split: 1 of 4 UI reqs satisfied (UI-04), 3 of 4 partial. Combined with backend's 14 (MET 5 + API 5 + ING 4): **15 satisfied + 3 partial = 16/18 audit-credit**. The 3 partial are the 3 UI items deferred per autonomous-mode policy.

## Cross-phase integration

N/A — v1.2 contains only Phase 11. Within-phase integration:
- Schema → API → Ingest → Commands → Frontend wiring verified by `cargo build --lib` and `pnpm tsc --noEmit` both succeeding
- 6 new IPC commands registered correctly in `lib.rs::generate_handler!`

## Tech debt summary

5 items, all explicitly carried (no surprises):

1. Cross-source person dedup (v1.3 seed)
2. Persons aggregate page enrichment (timeline/co-staff) — `seeds/persons-page-enrichment.md`
3. Person portrait local caching
4. Backfill progress UI minimal (events only)
5. tauri-plugin-opener not added (cmd /C start fallback acceptable for Windows-only constraint)

## Nyquist Compliance

Skipped — `workflow.nyquist_validation: false` in `.planning/config.json`.

## Decision

Per autonomous-mode policy (`.planning/.../feedback_no_questions_in_autonomous.md`):
**Accept human-needed gaps; proceed to /gsd-complete-milestone v1.2.**

The 3 deferred UI requirements are expected to surface during the next dev-server session; if any fails, the user can roll back via /gsd-undo or a quick patch phase. None of the deferred items represent unimplemented code paths — they all type-check and the wiring is mechanically correct; only visual/interaction polish is unverified.

## Next

```
/gsd-complete-milestone v1.2
```
