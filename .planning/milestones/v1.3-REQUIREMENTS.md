# Requirements: v1.3 Scan Pipeline & Person Polish (ARCHIVED)

**Milestone:** v1.3 — Scan Pipeline & Person Polish
**Defined:** 2026-05-12
**Shipped:** 2026-05-12
**Core Value:** 让本地一堆乱糟糟的 galgame 目录，变成可搜索、可启动、可统计的图书馆——并且每张卡片背后都有充实的元数据。v1.3 把 v1.1/v1.2 累积的 carry-over 一次性清掉，让用户首次"扫描-复核-绑定-跳转"的体验跑通端到端。

## v1.3 Requirements (17 total — all code-shipped)

### SCAN (Scan Pipeline & Review Queue)

- [x] **SCAN-01**: 新增独立 `/scan` 路由（无 Library 侧栏，顶部窄 nav 返回 Library），页面顶部 KPI 4 联：已扫游戏 / 已绑定 / 待复核 / 不匹配；增量扫描 / 全量重扫 / 取消 三按钮 → **Phase 12 (real-app smoke → v1.4)**
- [x] **SCAN-02**: 中央双栏 feed —— 左栏实时增量扫描日志（最新 200 条 + 自动滚动），右栏「待复核」卡片队列；写入新表 `scan_review_queue` 持久化 → **Phase 12 (real-app smoke → v1.4)**
- [x] **SCAN-03**: 待复核卡片展开 → Bangumi vs VNDB 并排候选对比，一键采用并触发 ingest 重抓 → **Phase 12 (real-app smoke → v1.4)**

### PER (Person Page Enrichment)

- [x] **PER-01**: 跨源人物去重 —— `/persons/:id` 与 Detail staff chip 上同 name 同 role 的 Bangumi/VNDB persons 折叠为一行；DB 层保留双源行，仅在 IPC 查询时归并 → **Phase 13a (93409c9, 4 unit tests)**
- [x] **PER-02**: `/persons/:id` 顶部「作品时光轴」—— release_year 横向排布，气泡尺寸映射 playtime（log scale），hover Tooltip → **Phase 13b (c18bc90)**
- [x] **PER-03**: `/persons/:id` 底部「常与 X 共同出现」横滑条 —— SQL JOIN game_staff × game_staff，HAVING coshare >= 2，role_hint 子查询 → **Phase 13c (36cb5ac)**
- [x] **PER-04**: 人物头像本地缓存 —— `data/portraits/{source}-{source_id}.jpg`，Bangumi `/v0/persons/{id}` images.medium；新增 `get_or_fetch_portrait` IPC → **Phase 13d (a7f627c, VNDB 推迟到 v1.4)**

### FS (Filesystem Actions via tauri-plugin-opener)

- [x] **FS-01**: 引入 `tauri-plugin-opener = "2"`；新 `open_path(path)` IPC；`open_in_explorer` / `open_external_url` 内部 delegate 到 opener；capabilities default.json 增 3 行权限 → **Phase 14a (64b2695)**
- [x] **FS-02**: GameCard ContextMenu 「打开目录」（Detail 已在 quick task 20260509b 有）→ **Phase 14b (b1e1932)**
- [x] **FS-03**: Screenshots 每组 header 「打开目录」按钮 → **Phase 14c (b1e1932)**

### POL (Polish & Cross-cutting Carry-over)

- [x] **POL-01**: Detail 页 `?tab=screenshots|saves|notes|sessions|config|overview` deeplink；非法值 fallback overview；切换 tab 写回 URL（`replace`）→ **Phase 14d (f2fa535)**
- [x] **POL-02**: `get_session_count() -> i64` IPC（`SELECT COUNT(*) FROM sessions WHERE ended_at IS NOT NULL`，schema 列名是 `ended_at` 不是 `end_at`）+ Stats.tsx 集成 → **Phase 14e (0dceb09)**
- [x] **POL-03**: Backfill 进度 UI —— BackfillProgressBar 监听 `meta-fetch-progress-meta` + `meta-fetch-progress`；新 `cancel_backfill` IPC；BackfillState AtomicBool → **Phase 13e (038fdd0)**
- [x] **POL-04**: LIB-02 杂志式不对称网格 — **废止决策** 写入 PROJECT.md Key Decisions（含原因 + 最终方案 auto-fill 均匀网格）→ **Phase 14f (61e9290)**

### VER (v1.2 Real-app Smoke Verification)

- [x] **VER-01**: UI-01 真机验证 → **Phase 15 walkthrough V-01 (待 v1.4 audit 执行)**
- [x] **VER-02**: UI-02 真机验证 → **Phase 15 walkthrough V-02 (待 v1.4 audit 执行)**
- [x] **VER-03**: UI-03 真机验证 → **Phase 15 walkthrough V-03 (待 v1.4 audit 执行)**

## Outcomes

**Validated (shipped + code verified):**
- SCAN-01/02/03 — `/scan` 路由 + ReviewQueue + 候选对比 (Phase 12)
- PER-01/02/03/04 — 人物聚合页二阶加强 (Phase 13)
- FS-01/02/03 + POL-01/02/03/04 — 文件系统 + Detail polish + LIB-02 决策 (Phase 13/14)

**Adjusted:**
- VER-01/02/03 — code present, real-app smoke 推迟到 v1.4 milestone audit；Phase 15 交付 12-step walkthrough doc (V-01..V-12)
- PER-04 portrait — 只支持 Bangumi；VNDB GraphQL person query 推迟到 v1.4

**Dropped:**
- 无（所有 v1.3 requirements 都有 code commit；只是 real-app smoke 状态分了"自动化 vs 真机"两层）

## Carry-over to v1.4

- **walkthrough V-01..V-12** 真机执行（最高优先级——blocking v1.4 第一件事）
- VNDB portrait 抓取（PER-04 二期）
- 跨源 persons 物理合并（迁移 game_staff FK；用户感知度提高后再做）
- co-staff 复杂权重（voice ↔ scenario 的 affinity）
- 完成度 chip（"5 部 / 已通关 3 / 47 h"）
- 自动定时 backfill / portrait CDN 代理 / Stats 会话分组 KPI
- 5 个 pre-existing cargo warnings 清理

---

_See also: [v1.3-ROADMAP.md](v1.3-ROADMAP.md) · [v1.3-MILESTONE-AUDIT.md](v1.3-MILESTONE-AUDIT.md)_
