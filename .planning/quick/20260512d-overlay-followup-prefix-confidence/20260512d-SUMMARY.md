---
gsd_quick_version: 1.0
slug: overlay-followup-prefix-confidence
date: 2026-05-12
status: complete
---

# Quick 20260512d — Summary

20260512c 的两个 followup 反馈：
1. alert-dialog 也用了独立 Overlay，未跟 20260512c 同步加深
2. `アマエミDL版` clean 后 `アマエミ` 是 VNDB 完整标题的前缀，score 76 < 80，不自动绑

## 改动

### alert-dialog overlay
`src/components/ui/alert-dialog.tsx` overlay 从 `bg-black/10 ... backdrop-blur-xs` 改为 `bg-black/60 ... backdrop-blur-sm`，和 dialog.tsx 对齐。

### match_score 前缀置信度
`src-tauri/src/metadata/match_score.rs` containment 分支：
- 短边 ≥ 3 字符 且 一边是另一边前缀 → baseline 80（原来 70）
- 否则保持 70 baseline（防止 "A" 命中 "ABCDEFG"）

具体：`70 + coverage * 29` → 分两路：
- prefix 路径：`80 + coverage * 19`
- 非 prefix 路径：`70 + coverage * 29`

`アマエミ` vs `アマエミ ～甘やかさせて♥もっとデキてる彼女～`：
- coverage ≈ 0.22
- 旧：70 + 0.22*29 = 76
- 新：80 + 0.22*19 = 84 ✓ 过 80 阈值

### 新测试 3 个
- 前缀命中阈值（用户场景）
- 短前缀（2 chars）不 promote 仍 70 路径
- 非前缀 containment 走 70 路径

## Verification

- `cargo test --lib`：76/76 ✅
- `pnpm tsc --noEmit`：✅

## 用户操作建议

历史已写库的低置信度游戏不会自动重评分。要触发重算：
- **逐个**：在 Scan 页的 ReviewQueue 里点 "采用 VNDB 候选" → 走 `bind_metadata` 路径
- **批量**：到设置页 → 全部刷新元数据 → 所有游戏重跑 `score` 计算

新扫描的游戏直接受益。

## Files touched

- src/components/ui/alert-dialog.tsx (1 行)
- src-tauri/src/metadata/match_score.rs (containment 分支重写 + 注释 + 3 测试)
