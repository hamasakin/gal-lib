---
quick_id: 260513-2nx
slug: year-backfill-replace-enrich
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/commands.rs
  - src-tauri/src/lib.rs
  - src/lib/persons.ts
  - src/routes/Settings.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "Settings「扫描操作」区有新按钮「补全发行年份」，点击后 BackfillProgressBar 出现并走完进度"
    - "旧按钮「补全简介/作者/声优/官方标签」已从 Settings 移除（DOM 中搜不到该字符串）"
    - "调用新 IPC `backfill_release_year` 后，DB 里 `release_year IS NULL AND (bangumi_id IS NOT NULL OR vndb_id IS NOT NULL)` 的游戏行被回灌（年份非 NULL）"
    - "新 IPC 只更新 release_year + last_scanned_at，不动 summary/brand/age_rating/tags/staff（对 manual 绑定无损）"
    - "旧 IPC `backfill_metadata_enrichment` 整套代码（Rust fn + lib.rs 注册行 + TS wrapper + Settings handler）全部消失，grep 不到"
    - "`cargo check` 通过；`pnpm tsc --noEmit` 通过"
  artifacts:
    - path: "src-tauri/src/commands.rs"
      provides: "新增 pub async fn backfill_release_year；删除 pub async fn backfill_metadata_enrichment"
      contains: "pub async fn backfill_release_year"
    - path: "src-tauri/src/lib.rs"
      provides: "invoke_handler! 里 `commands::backfill_release_year` 替换原 `commands::backfill_metadata_enrichment` 注册行"
      contains: "commands::backfill_release_year"
    - path: "src/lib/persons.ts"
      provides: "新增 `backfillReleaseYear()` wrapper；删除 `backfillMetadataEnrichment()`"
      contains: "export async function backfillReleaseYear"
    - path: "src/routes/Settings.tsx"
      provides: "import + handler + 按钮改名为「补全发行年份」；lede + AlertDialog 文案同步更新"
      contains: "补全发行年份"
  key_links:
    - from: "src/routes/Settings.tsx 按钮 onClick"
      to: "backfillReleaseYear() in src/lib/persons.ts"
      via: "import + handler"
      pattern: "backfillReleaseYear\\(\\)"
    - from: "src/lib/persons.ts backfillReleaseYear"
      to: "Rust `backfill_release_year` command"
      via: "invoke(\"backfill_release_year\")"
      pattern: "invoke\\(\"backfill_release_year\""
    - from: "src-tauri/src/commands.rs backfill_release_year"
      to: "metadata::bangumi::fetch_detail / metadata::vndb::fetch_detail"
      via: "直接按 source_id 拉详情（不重做模糊匹配）"
      pattern: "metadata::(bangumi|vndb)::fetch_detail"
    - from: "src-tauri/src/commands.rs backfill_release_year"
      to: "ingest::parse_release_year(d.release_date.as_deref())"
      via: "复用 quick 20260512b 加的解析器"
      pattern: "ingest::parse_release_year"
    - from: "BackfillProgressBar.tsx (未改)"
      to: "新 IPC 发出的 `meta-fetch-progress-meta` / `meta-fetch-progress` 事件"
      via: "事件协议沿用 — total/done/cancelled + started/finished"
      pattern: "meta-fetch-progress"
---

<objective>
新增 `backfill_release_year` 专用 IPC + Settings「补全发行年份」按钮，**同时彻底删除**旧的 `backfill_metadata_enrichment` 全套代码（Rust fn + Tauri 注册 + TS wrapper + Settings handler/按钮/AlertDialog 文案）。

Purpose: 历史绑定的游戏 release_year 大多为 NULL（quick 20260512b 之前 5 处 UPDATE 路径不写）；现有 enrichment backfill 的 SQL filter（`NOT IN game_staff`）跳过已 enrich 但年份缺失的行；`refresh_all_metadata` 会重做模糊匹配，对 manual 绑定有损。需要一个**只回灌年份、按 source_id 直连 fetch_detail**的精准命令。旧 enrichment 按钮的职责已经被 v1.3 的 enrichment 主流程覆盖，留着只是混淆 UI。

Output:
- 后端：新 IPC + 注册；旧 IPC 全套代码删除
- 前端：新 wrapper + 新按钮文案；旧 wrapper + handler + 按钮 + 相关 lede / AlertDialog 提示文案全部清理
- `cargo check` + `pnpm tsc --noEmit` 全绿
- 手动 smoke：Settings 页有新按钮无旧按钮，点击触发 BackfillProgressBar，DB 历史 NULL 行被回灌
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@D:/project/gal-lib/CLAUDE.md
@D:/project/gal-lib/.planning/STATE.md

<interfaces>
<!-- 从 codebase 提取的关键签名 — executor 直接使用，不要重新探索 -->

From `src-tauri/src/metadata/types.rs`:
```rust
pub enum MetadataSource { Bangumi, Vndb, Manual, None }

pub struct MetadataDetail {
    pub source: MetadataSource,
    pub source_id: String,
    pub title: String,
    pub title_cn: Option<String>,
    pub cover_url: Option<String>,
    pub summary: Option<String>,
    pub release_date: Option<String>,   // <-- 这是关键字段
    pub brand: Option<String>,
    pub tags: Vec<OfficialTagRef>,
    pub is_r18: Option<bool>,
}
```

From `src-tauri/src/metadata/bangumi.rs:85` 和 `src-tauri/src/metadata/vndb.rs:111`:
```rust
pub async fn fetch_detail(bangumi_id: &str) -> Result<MetadataDetail, MetadataError>;
pub async fn fetch_detail(vndb_id: &str) -> Result<MetadataDetail, MetadataError>;
```

From `src-tauri/src/ingest.rs:90`:
```rust
pub fn parse_release_year(s: Option<&str>) -> Option<i32>;
```

From `src-tauri/src/commands.rs:74`（已存在，**保留**，新 IPC 复用）:
```rust
pub struct BackfillState {
    pub cancel: std::sync::atomic::AtomicBool,
}
```

From `src-tauri/src/commands.rs`（已存在的引用，可沿用）:
```rust
use crate::launch::{le, orchestrator, process_track, session};
use crate::metadata::types::MetadataSource;
use crate::{ingest, metadata, scan, AppPaths};
use sqlx::{Row, SqlitePool};
use tauri::{AppHandle, Emitter, Manager, State};
```

From `src/lib/persons.ts`（已存在的 wrapper 模式，新 wrapper 照葫芦画瓢）:
```ts
export async function cancelBackfill(): Promise<void> {
  await invoke("cancel_backfill");
}
// 事件 payload 类型保留不动：
export interface MetaFetchProgressMeta { total?: number; done?: boolean; cancelled?: boolean; }
export interface MetaFetchProgress { game_id: number; phase: "started" | "finished"; name?: string; }
```

BackfillProgressBar（`src/components/library/BackfillProgressBar.tsx`，**不要改**）监听：
- `meta-fetch-progress-meta`：`{ total }` 开始 / `{ cancelled: true }` 取消 / `{ done: true }` 完成
- `meta-fetch-progress`：`{ game_id, phase: "started"|"finished", name }`
新 IPC 必须按这套协议发事件。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 后端 — 新增 backfill_release_year + 删除 backfill_metadata_enrichment</name>
  <files>
    src-tauri/src/commands.rs,
    src-tauri/src/lib.rs
  </files>
  <action>
**Part A — 在 `src-tauri/src/commands.rs` 中新增 `backfill_release_year`（建议放在被删的 `backfill_metadata_enrichment` 原位置，与 `cancel_backfill` 紧邻，保持代码区块的语义聚合）：**

新函数必须满足：
1. 签名：`pub async fn backfill_release_year(app: AppHandle, state: State<'_, AppPaths>, backfill_state: State<'_, BackfillState>) -> Result<(), String>` —— 与被删的旧函数一致，方便复用现有 BackfillState 管理。
2. SQL（仅这一条 SELECT，**不要**加 `NOT IN (SELECT game_id FROM game_staff)` 过滤）：
   ```sql
   SELECT id, name, bangumi_id, vndb_id, metadata_source FROM games
   WHERE release_year IS NULL
     AND (bangumi_id IS NOT NULL OR vndb_id IS NOT NULL)
   ```
3. 空结果直接 `return Ok(())`。
4. 行为复刻被删旧函数的事件 / cancel / spawn 框架，但 **fetch 与 update 必须精简**：
   - 重置 cancel flag，emit `meta-fetch-progress-meta { "total": <rows.len()> }`。
   - `tokio::spawn` 异步循环；每次迭代开头检查 `cancel`，命中时 emit `meta-fetch-progress-meta { "cancelled": true }` 并 break。
   - 解出 `game_id: i64`, `game_name: String`, `bangumi_id: Option<String>`, `vndb_id: Option<String>`, `metadata_source: Option<String>`。
   - 按 metadata_source 选源（与旧函数的 match 块完全一致）：`"bangumi"` 用 bangumi_id，`"vndb"` 用 vndb_id，`"manual"` / `None` / 其它 fallback 到「先 bangumi_id 后 vndb_id」。无可用 id 则 `continue`。
   - emit `meta-fetch-progress { "game_id": ..., "phase": "started", "name": <game_name> }`。
   - **只调一次** `metadata::bangumi::fetch_detail(&source_id).await` 或 `metadata::vndb::fetch_detail(&source_id).await`（按 source_enum 分支）。**不要**调 fetch_persons / fetch_characters。
   - 失败时 eprintln 一行 `[backfill-year] fetch_detail failed for game {id} ({:?}): {err}`，然后 emit finished 后 `continue`（不要 panic）。
   - 成功时 `let year = ingest::parse_release_year(d.release_date.as_deref());`。
   - 若 `year.is_some()`，执行 **唯一**这条 UPDATE：
     ```sql
     UPDATE games SET release_year = ?, last_scanned_at = datetime('now') WHERE id = ?
     ```
     bind `year as i64` 与 `game_id`。**绝对不要**写 summary / brand / age_rating / tags / staff —— 这是与旧 backfill 行为分道扬镳的核心点（manual 绑定零损伤）。
   - 若 `year.is_none()`（详情里就没年份），跳过 UPDATE，仍发 finished 事件。
   - emit `meta-fetch-progress { "game_id": ..., "phase": "finished", "name": <game_name> }`。
   - 循环结束后 emit `meta-fetch-progress-meta { "done": true }`（与旧函数一致，无论是否中途 cancel 都补一发）。
5. 顶部 doc-comment 写清楚：仅回灌 release_year、按 source_id 直连 fetch_detail（不重做模糊匹配，对 manual 绑定无损）、复用 BackfillState 与既有事件协议、可被 cancel_backfill 中止。

**Part B — 在 `src-tauri/src/commands.rs` 中删除 `backfill_metadata_enrichment`：**

完整删除 `pub async fn backfill_metadata_enrichment(...)` 整个函数体（约原 line 3485-3693，包括其上方 `///` 文档注释起到函数右大括号 `}` 止）。`cancel_backfill`（紧随其后）必须保留。

**Part C — 在 `src-tauri/src/lib.rs` 中替换 invoke_handler! 注册：**

第 253 行附近 `commands::backfill_metadata_enrichment,` 行 → 改为 `commands::backfill_release_year,`。注释 `// 11c — Phase 11 metadata enrichment IPCs (6 new)` 上方块若直接影响阅读，可顺手把对应行尾或上方注释微调（非必须）。`commands::cancel_backfill,`（第 258 行附近）保留不动。

**Part D — verify：**

运行 `cargo check --manifest-path src-tauri/Cargo.toml`。必须零 error；任何 warning 若是新增代码引入的，须修掉（注意 `unused_imports` —— 新函数没有再用到的引用旧函数也别留）。
  </action>
  <verify>
    <automated>cd src-tauri && cargo check 2>&1 | tail -40</automated>
    <automated>cd D:/project/gal-lib && grep -n "backfill_metadata_enrichment\|backfill_release_year" src-tauri/src/commands.rs src-tauri/src/lib.rs</automated>
  </verify>
  <done>
- `cargo check` 通过（0 error，0 warning from new code）
- `grep backfill_metadata_enrichment src-tauri/src/` 完全为空（fn + 注册行均消失）
- `grep "pub async fn backfill_release_year" src-tauri/src/commands.rs` 命中 1 行
- `grep "commands::backfill_release_year" src-tauri/src/lib.rs` 命中 1 行
- `grep "commands::cancel_backfill" src-tauri/src/lib.rs` 仍命中 1 行（未误删）
- 新函数体内 SQL UPDATE 只有一条且仅写 `release_year` + `last_scanned_at`（不写 summary/brand/age_rating/tags 等列）
  </done>
</task>

<task type="auto">
  <name>Task 2: 前端 — 替换 wrapper / Settings 按钮与文案</name>
  <files>
    src/lib/persons.ts,
    src/routes/Settings.tsx
  </files>
  <action>
**Part A — `src/lib/persons.ts`：**

1. 删除现有 `export async function backfillMetadataEnrichment(): Promise<void>` 整个函数（连同上方多行 doc-comment）。
2. 在原位置新增：
   ```ts
   /**
    * 补全所有已绑定（bangumi_id 或 vndb_id 非空）但 release_year 为 NULL 的
    * 游戏年份。按 source_id 直连 fetch_detail（不重做模糊匹配），只写
    * release_year + last_scanned_at —— 对 manual 绑定零损伤。
    *
    * Fire-and-forget；进度走现有 `meta-fetch-progress-meta` /
    * `meta-fetch-progress` 事件通道（BackfillProgressBar 已订阅）。
    * 可通过 `cancelBackfill()` 中止。
    */
   export async function backfillReleaseYear(): Promise<void> {
     await invoke("backfill_release_year");
   }
   ```
3. 模块顶部 doc-comment（line 1-15 注释块）里那段把 6 个 Phase 11 命令逐一列举的 “- `backfill_metadata_enrichment()` …” 行可顺手替换为 “- `backfill_release_year()` — 补全发行年份”（保持文档与导出一致；非必须，但能避免后续阅读疑惑）。
4. `cancelBackfill` / `MetaFetchProgressMeta` / `MetaFetchProgress` 等类型与导出**完全保留**。

**Part B — `src/routes/Settings.tsx`：**

1. 第 58 行 import 替换：
   - 旧：`import { backfillMetadataEnrichment } from "@/lib/persons";`
   - 新：`import { backfillReleaseYear } from "@/lib/persons";`
2. 第 269-277 行的 `onBackfillEnrichment` handler 改名 + 改实现：
   ```ts
   async function onBackfillReleaseYear() {
     try {
       await backfillReleaseYear();
       toast.info("已开始补全发行年份 — 后台运行，受 API 限速影响可能耗时数分钟");
       navigate("/");
     } catch (e: unknown) {
       toast.error(`启动失败 — ${String(e)}`);
     }
   }
   ```
3. 第 492-497 行 Section `id="scan-ops"` 的 `lede` 属性：把句尾 “· 补全简介/作者/声优只对已绑定但缺 staff 的游戏拉取（不改绑定）” 替换为 “· 补全发行年份只对历史绑定但缺年份的游戏拉取（不改其它字段）”。
4. 第 505-507 行按钮：
   - 旧：`<SettingButton onClick={() => void onBackfillEnrichment()}>补全简介/作者/声优/官方标签</SettingButton>`
   - 新：`<SettingButton onClick={() => void onBackfillReleaseYear()}>补全发行年份</SettingButton>`
5. 第 518 行 AlertDialog 描述（「强制刷新全部元数据」弹窗内）末段 “若只想补全简介/作者/声优数据而不改绑定，请使用「补全简介/作者/声优」按钮。” 替换为 “若只想给历史绑定补回发行年份（不改其它字段），请使用「补全发行年份」按钮。”

**Part C — verify：**

运行 `pnpm tsc --noEmit`。必须零 error。
运行 grep 确认两个旧标识符在整个 `src/` 下绝迹：
- `backfillMetadataEnrichment`
- `onBackfillEnrichment`
- 中文字符串 `补全简介/作者/声优`（在 Settings.tsx 出现 3 处：lede / 按钮 / AlertDialog —— 全部要换掉）
  </action>
  <verify>
    <automated>cd D:/project/gal-lib && pnpm tsc --noEmit 2>&1 | tail -30</automated>
    <automated>cd D:/project/gal-lib && grep -rn "backfillMetadataEnrichment\|onBackfillEnrichment\|补全简介" src/ || echo "OK: 旧标识符已清除"</automated>
    <automated>cd D:/project/gal-lib && grep -n "backfillReleaseYear\|补全发行年份" src/lib/persons.ts src/routes/Settings.tsx</automated>
  </verify>
  <done>
- `pnpm tsc --noEmit` 通过
- `grep -rn "backfillMetadataEnrichment\|onBackfillEnrichment\|补全简介" src/` 返回 “OK: 旧标识符已清除”（即无任何匹配）
- `grep backfillReleaseYear src/lib/persons.ts` 命中（导出存在）
- `grep backfillReleaseYear src/routes/Settings.tsx` 命中（import + handler 调用）
- `grep 补全发行年份 src/routes/Settings.tsx` 命中至少 2 次（按钮文案 + AlertDialog 提示）
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 人工 smoke — 启动应用验证按钮 / 进度条 / DB 回灌</name>
  <what-built>
新增 `backfill_release_year` IPC + Settings「补全发行年份」按钮，删除旧 `backfill_metadata_enrichment` 全套代码。点击新按钮应当：
1. 触发 BackfillProgressBar 出现
2. 按现有事件协议显示 total / current 进度
3. 完成后，DB 中所有「有 bangumi_id 或 vndb_id 但 release_year IS NULL」的行被回灌（部分行可能因源端就无年份而保持 NULL）
4. 不影响 manual 绑定的 summary/brand/staff 等字段
  </what-built>
  <how-to-verify>
1. `cd D:/project/gal-lib && pnpm tauri dev`（启动开发版应用）。
2. 进入 Settings 页 → 「扫描操作」节。
   - **检查按钮**：应看见「补全发行年份」按钮；不应再看见「补全简介/作者/声优/官方标签」按钮。
   - **检查 lede**：「扫描操作」节副标题描述里出现「补全发行年份」字样，不再有「补全简介/作者/声优」。
3. （可选） 用 SQLite 工具（DB 路径 `data/library.sqlite`，或直接 `sqlite3` 命令行）记录基线：
   ```sql
   SELECT COUNT(*) FROM games
   WHERE release_year IS NULL
     AND (bangumi_id IS NOT NULL OR vndb_id IS NOT NULL);
   -- 记为 N0
   ```
4. 点击「补全发行年份」按钮：
   - 应弹 toast「已开始补全发行年份 — 后台运行…」
   - 跳转 Library 页后顶部 BackfillProgressBar 应出现
   - 应能看到 `补齐元数据 — {游戏名}` + `current / total · %` 实时滚动
   - 「取消」按钮应可用（点 → AlertDialog 确认 → 中止）
5. 等 BackfillProgressBar 自动消失（成功完成 → 5s 后自动隐藏；或主动取消）。
6. 再次查询 DB：
   ```sql
   SELECT COUNT(*) FROM games
   WHERE release_year IS NULL
     AND (bangumi_id IS NOT NULL OR vndb_id IS NOT NULL);
   -- 应 << N0（源端能给出年份的行已被回灌）
   ```
7. （可选）抽查一两个 manual 绑定的游戏（`metadata_source = 'manual'`）：
   ```sql
   SELECT id, name, metadata_source, summary, brand, release_year FROM games WHERE metadata_source = 'manual' LIMIT 3;
   ```
   summary / brand 应未变；若该行原 release_year 为 NULL 且 Bangumi/VNDB 详情有年份，则 release_year 已被填上 —— 这是预期行为。

**预期结果汇总：**
- [ ] 旧按钮「补全简介/作者/声优/官方标签」消失
- [ ] 新按钮「补全发行年份」出现并可点击
- [ ] BackfillProgressBar 正常工作（出现 / 进度 / 取消 / 自动消失）
- [ ] DB 历史 NULL 行被回灌（数量明显下降）
- [ ] manual 绑定的非年份字段未被覆盖
  </how-to-verify>
  <resume-signal>输入「approved」或描述实际看到的问题</resume-signal>
</task>

</tasks>

<verification>
- 后端：`cargo check`（src-tauri/） 0 error
- 前端：`pnpm tsc --noEmit` 0 error
- 死代码清除：`grep -rn backfill_metadata_enrichment src-tauri/src/ src/` 完全无匹配
- 死代码清除：`grep -rn "backfillMetadataEnrichment\|onBackfillEnrichment\|补全简介" src/` 完全无匹配
- 新代码就位：`grep backfill_release_year` 在 commands.rs / lib.rs / persons.ts 各命中 ≥1
- 人工 smoke 通过（Task 3）
</verification>

<success_criteria>
- [ ] 后端新 IPC `backfill_release_year` 实现完成、注册到 invoke_handler!
- [ ] 后端旧 IPC `backfill_metadata_enrichment` 函数体与注册行全部删除
- [ ] 前端新 wrapper `backfillReleaseYear` 导出
- [ ] 前端旧 wrapper / handler / 按钮 / lede / AlertDialog 文案全部更新
- [ ] BackfillState + cancel_backfill 保留未动
- [ ] BackfillProgressBar 组件 0 改动
- [ ] cargo check + pnpm tsc --noEmit 双绿
- [ ] 人工 smoke：旧按钮消失 / 新按钮工作 / DB 回灌生效 / manual 绑定无副作用
</success_criteria>

<output>
After completion, create `.planning/quick/260513-2nx-year-backfill-replace-enrich/260513-2nx-SUMMARY.md`
</output>
