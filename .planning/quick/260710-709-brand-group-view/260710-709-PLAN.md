---
phase: quick-260710-709
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/library/GameGroupGrid.tsx
  - src/lib/preferences.ts
  - src/components/library/ViewToggle.tsx
  - src/routes/Library.tsx
autonomous: true
requirements: [BRAND-GROUP-VIEW]

must_haves:
  truths:
    - "库页面工具栏的视图切换器出现第三个选项『分组』，点击后切到品牌分组视图"
    - "分组视图里游戏卡片按 brand（开发商）一组一组展示，每组顶部有品牌名标题"
    - "组内仍是卡片网格，卡片外观/密度与普通网格视图一致"
    - "brand 为空/null 的游戏归入『未知品牌』组，且该组排在最后"
    - "切换到分组视图后偏好被持久化（刷新应用仍是分组视图）"
  artifacts:
    - path: "src/components/library/GameGroupGrid.tsx"
      provides: "按品牌分组渲染 GameCard 网格的新组件"
      min_lines: 60
    - path: "src/lib/preferences.ts"
      provides: "ViewMode 增加 'group' 成员 + VIEW_MODES 数组更新"
      contains: "group"
    - path: "src/components/library/ViewToggle.tsx"
      provides: "三段式视图切换器（网格/列表/分组）"
    - path: "src/routes/Library.tsx"
      provides: "viewMode === 'group' 渲染分支 + 分组视图下显示 DensityToggle"
  key_links:
    - from: "src/routes/Library.tsx"
      to: "src/components/library/GameGroupGrid.tsx"
      via: "viewMode === 'group' 条件渲染，透传 visibleGames + 卡片回调"
      pattern: "viewMode === \"group\""
    - from: "src/components/library/ViewToggle.tsx"
      to: "src/store/preferences.ts"
      via: "setViewMode('group')"
      pattern: "group"
---

<objective>
在库页面（Library route，"/"）新增第三种视图模式「分组」：把网格卡片按品牌（brand / 开发商）一组一组分组展示，每组一个品牌名标题，组内仍是卡片网格。用户在工具栏视图切换器里切到该模式后生效，偏好持久化。

Purpose: 让用户能以「按厂商归类」的方式浏览收藏——同一开发商的作品聚在一起，比纯扁平网格更有图书馆感。
Output: 新增 `GameGroupGrid.tsx` 组件 + `ViewMode` 类型扩展 + `ViewToggle` 三段化 + `Library.tsx` 渲染分支。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

现有实现关键点（已勘查，执行时无需再探索）：

- **视图模式定义** `src/lib/preferences.ts:12`
  ```ts
  export type ViewMode = "grid" | "list";
  export const VIEW_MODES: ViewMode[] = ["grid", "list"];
  ```
  `isViewMode` 校验器读 `VIEW_MODES`，`applyPreferences` 把 viewMode 写到 `<html data-view>`。加成员后 loadPreferences 的校验自动兼容，无需改校验函数。持久化走 localStorage `gal-lib:prefs`（已有）。

- **偏好 store** `src/store/preferences.ts` — `setViewMode(v)` 已存在，`update()` 内 `savePreferences` + `applyPreferences` 自动处理持久化。新增视图模式零改动此文件。

- **视图切换器** `src/components/library/ViewToggle.tsx` — 硬编码中文 label（`网格`/`列表`），非 i18n。段式 radiogroup，`OPTIONS` 数组驱动。**注意：本组件不走 i18n，保持硬编码中文即可，不要动三份 translation.json（避免 verify-i18n 键对齐 churn）。**

- **Library 渲染分支** `src/routes/Library.tsx`：
  - L152 `const viewMode = usePreferencesStore((s) => s.viewMode);`
  - L500 `const filteredGames = applyAdvancedFilter(games, advFilter);` → L559 `const visibleGames = filteredGames;`（已 server 排序 + 客户端高级过滤后的最终列表）
  - L700 `{viewMode === "grid" && <DensityToggle />}`
  - L745-757 `{!isEmpty && viewMode === "grid" && (<GameGrid .../>)}`
  - L758 `{!isEmpty && viewMode === "list" && <GameList games={visibleGames} />}`
  - 卡片回调：`setPickerGame`（onPickMetadata）、`onSplitSubdirs`、`onRequestDelete`、`onChildMutation`。批量选择（selectMode）仅 grid 用，分组视图不接入批量选择（保持简单）。
  - 滚动容器 `scrollContainerRef`（L706 `overflow-auto` 的 div）包裹渲染区——分组视图非虚拟化，直接渲染进这个滚动容器即可，**不需要** scrollContainerRef。

- **Game 类型 brand 字段** `src/lib/games.ts:65` `brand: string | null;`（品牌/开发商，来自元数据源；可为 null）。

- **GameCard 组件** `src/components/library/GameCard.tsx` props 契约（L43-65）：
  ```ts
  game: Game; coverDataUrl: string | null;
  onPickMetadata: (game) => void; onRefreshCover: (game) => void;
  onSplitSubdirs: (game) => void; onRequestDelete: (game) => void;
  onMutated?: () => void;
  selectMode?: boolean; selected?: boolean; onToggleSelect?: (id) => void;  // 可选，分组视图不传
  ```

- **封面 URL 解析**（现存于 `GameGrid.tsx:76-98`，需在新组件内复刻这段自包含逻辑）：
  ```ts
  const [dataDir, setDataDir] = useState<string | null>(null);
  useEffect(() => { invoke<string>("get_data_dir").then(setDataDir).catch(...); }, []);
  const resolveCover = (game: Game): string | null => {
    if (game.cover_path && dataDir) {
      const abs = `${dataDir.replace(/\\/g, "/")}/${game.cover_path}`;
      return convertFileSrc(abs) + `?v=${encodeURIComponent(game.last_scanned_at ?? "")}`;
    }
    return game.cover_url ?? null;
  };
  ```

- **密度变量** `src/index.css:130-137`：`[data-density="small|medium|large"] { --card-w: 132/172/224px; }` 挂在 `<html>`，CSS 自定义属性向下继承——分组视图可直接用 CSS grid `repeat(auto-fill, minmax(var(--card-w), 1fr))` 响应密度，无需手动测列数（GameGrid 手测列数是因为它虚拟化；分组视图非虚拟化，用原生 auto-fill 更简单）。

- **列间距参考** `GameGrid.tsx:59-60`：`COLUMN_GAP = 22`、`ROW_GAP = 28`（保持视觉一致）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 GameGroupGrid 分组网格组件</name>
  <files>src/components/library/GameGroupGrid.tsx</files>
  <action>
新建 `src/components/library/GameGroupGrid.tsx`。这是一个自包含、非虚拟化的分组卡片网格组件。

Props 接口（与 Library 传入对齐）：
```ts
interface GameGroupGridProps {
  games: Game[];
  onPickMetadata: (game: Game) => void;
  onSplitSubdirs: (game: Game) => void;
  onRequestDelete: (game: Game) => void;
  onChildMutation?: () => void;
}
```

实现要点：
1. **dataDir + resolveCover**：复刻 context 中给出的自包含封面解析逻辑（`get_data_dir` 一次性拉取 + resolveCover 闭包，用 useMemo 依赖 dataDir）。import `convertFileSrc, invoke` from `@tauri-apps/api/core`。

2. **分组逻辑**（useMemo 依赖 games）：
   - 遍历 games，按 `game.brand` 归组；`brand` 为 null 或 trim 后为空 → 归入常量 key `UNKNOWN`（显示名 `未知品牌`）。
   - 组内游戏顺序：保持传入 games 的原始顺序（已是 server 排序 + 高级过滤后的结果，不要在组内重排）。
   - 组的排序：按品牌名 `localeCompare`（用 `"zh"` locale）升序；`未知品牌` 组恒定排在最后（无论字母序）。
   - 产出形如 `Array<{ key: string; label: string; games: Game[] }>`。

3. **onRefreshCover 回调**：GameCard 需要 `onRefreshCover`。复刻 GameGrid 的做法或简化为：调用 `refreshMetadata(game.id)` 后触发 `onChildMutation?.()`，失败 toast。可复用 `import { refreshMetadata } from "@/lib/metadata"` 与 `import { toast } from "sonner"`。onMutated 同样接 `onChildMutation`。

4. **渲染结构**：
   - 外层 `<div className="px-8 pb-20 pt-7 space-y-10">`（沿用 GameGrid 的 px-8/pb-20/pt-7 边距）。
   - 每个分组一个 `<section>`：
     - 品牌名标题：`<h2>` 或 `<div>`，样式参考项目已有标题风格——例如 `className="mb-4 flex items-baseline gap-2 font-serif text-[15px] text-ink-0"`，标题右侧可跟一个 mono 计数 `<span className="font-mono text-[11px] text-ink-3">{group.games.length}</span>`。用印章红短线/border-b 分隔更佳但非必须。
     - 卡片网格：
       ```tsx
       <div style={{
         display: "grid",
         gridTemplateColumns: "repeat(auto-fill, minmax(var(--card-w), 1fr))",
         columnGap: "22px",
         rowGap: "28px",
         alignItems: "start",
       }}>
       ```
       组内 `group.games.map` 渲染 `<GameCard key={g.id} game={g} coverDataUrl={resolveCover(g)} onPickMetadata={...} onRefreshCover={...} onSplitSubdirs={...} onRequestDelete={...} onMutated={onChildMutation} />`（不传 selectMode/selected/onToggleSelect）。

5. import `GameCard` from `./GameCard`、`type Game` from `@/lib/games`。
UI 文案硬编码中文（`未知品牌`），与 ViewToggle 硬编码风格一致，不引 i18n。
  </action>
  <verify>
    <automated>cd "D:/project/gal-lib" && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>GameGroupGrid.tsx 存在并通过 tsc；grep 确认包含 `未知品牌`、`repeat(auto-fill` 与 `localeCompare`。</done>
</task>

<task type="auto">
  <name>Task 2: 接线视图模式 —— ViewMode 类型 + ViewToggle 三段 + Library 渲染分支</name>
  <files>src/lib/preferences.ts, src/components/library/ViewToggle.tsx, src/routes/Library.tsx</files>
  <action>
把「分组」接入现有视图模式基础设施。三处改动：

**A. `src/lib/preferences.ts`**（约 L12、L61）：
- `export type ViewMode = "grid" | "list" | "group";`
- `export const VIEW_MODES: ViewMode[] = ["grid", "list", "group"];`
- 其余不动（`isViewMode` 读 VIEW_MODES 自动兼容；`DEFAULT_PREFS.viewMode` 保持 `"grid"`）。

**B. `src/components/library/ViewToggle.tsx`**：
- 在 `OPTIONS` 数组追加第三项：`{ value: "group", label: "分组", Icon: <合适的 lucide 图标> }`。
- 从 `lucide-react` import 一个表示「分组/按组」语义的图标，例如 `Boxes` 或 `Group`（`Boxes` 已存在于 lucide-react）。保持硬编码中文 label，与现有 `网格`/`列表` 风格一致。

**C. `src/routes/Library.tsx`**：
- import：`import { GameGroupGrid } from "@/components/library/GameGroupGrid";`
- DensityToggle 显示条件（现 L700 `{viewMode === "grid" && <DensityToggle />}`）改为分组视图也显示密度控制：`{(viewMode === "grid" || viewMode === "group") && <DensityToggle />}`（分组视图卡片同样受 `--card-w` 密度影响）。
- 在 list 渲染分支（现 L758）之后新增分组渲染分支：
  ```tsx
  {!isEmpty && viewMode === "group" && (
    <GameGroupGrid
      games={visibleGames}
      onPickMetadata={setPickerGame}
      onSplitSubdirs={onSplitSubdirs}
      onRequestDelete={onRequestDelete}
      onChildMutation={onChildMutation}
    />
  )}
  ```
- 批量选择按钮（L675 `{viewMode === "grid" && (...)}`）保持 grid-only，不改（分组视图不接入批量选择）。

不要触碰 GameGrid.tsx / GameList.tsx（它们的虚拟化逻辑与本任务无关）。
  </action>
  <verify>
    <automated>cd "D:/project/gal-lib" && pnpm exec tsc --noEmit && pnpm build</automated>
  </verify>
  <done>tsc + build 全绿；grep 确认 preferences.ts 含 `"group"`、ViewToggle 含 `分组`、Library.tsx 含 `viewMode === "group"` 与 `GameGroupGrid`。</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` 通过（无类型错误）。
- `pnpm build` 通过（Vite 打包成功）。
- grep 自验：
  - `grep -c '"group"' src/lib/preferences.ts` ≥ 1
  - `grep -c '分组' src/components/library/ViewToggle.tsx` ≥ 1
  - `grep -c 'viewMode === "group"' src/routes/Library.tsx` = 1
  - `grep -c 'GameGroupGrid' src/routes/Library.tsx` ≥ 2（import + 使用）
  - `grep -c '未知品牌' src/components/library/GameGroupGrid.tsx` ≥ 1
- GUI 真机验证（子代理跑不了 GUI，交付用户确认）：
  1. 工具栏视图切换器出现第三个「分组」按钮，点击后卡片按品牌分组、每组有品牌名标题。
  2. 组内卡片网格随密度切换（小/中/大）改变列宽。
  3. 无 brand 的游戏聚在「未知品牌」组且排在最后。
  4. 切到分组视图 → 重启应用 → 仍是分组视图（持久化）。
  5. 分组视图下卡片右键菜单（启动/元数据/删除/整理子目录）正常工作。
</verification>

<success_criteria>
- 库页面新增可切换、可持久化的「分组」视图模式。
- 分组视图按 brand 分组，每组品牌名标题 + 组内密度自适应卡片网格。
- null/空 brand 归「未知品牌」组并排在末尾。
- 复用现有 GameCard、密度变量、卡片回调，未破坏 grid/list 两种既有视图。
- tsc + build 全绿。
</success_criteria>

<output>
完成后创建 `.planning/quick/260710-709-brand-group-view/260710-709-SUMMARY.md`
</output>
