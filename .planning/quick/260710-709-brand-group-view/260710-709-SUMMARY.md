---
phase: quick-260710-709
plan: 01
subsystem: ui
tags: [react, tauri, library-view, brand-grouping, view-mode]

requires:
  - phase: quick-20260510b
    provides: ViewMode/viewMode 视图模式基础设施 + DensityToggle
provides:
  - 库页面第三种视图模式「分组」：按 brand 分组展示卡片网格
  - GameGroupGrid 非虚拟化分组网格组件
  - ViewToggle 三段化（网格/列表/分组）
affects: [library-view, game-card, preferences]

tech-stack:
  added: []
  patterns:
    - "非虚拟化分组网格：原生 CSS grid repeat(auto-fill, minmax(var(--card-w), 1fr)) 响应密度变量，无需手测列数"

key-files:
  created:
    - src/components/library/GameGroupGrid.tsx
  modified:
    - src/lib/preferences.ts
    - src/components/library/ViewToggle.tsx
    - src/routes/Library.tsx

key-decisions:
  - "分组视图非虚拟化：直接渲染进 Library 现有滚动容器，用 auto-fill 原生 grid 而非复刻 GameGrid 的 ResizeObserver 手测列数逻辑"
  - "未知品牌组用哨兵 key（前导空格 __unknown_brand__）标记并在排序中恒定置底，与真实品牌名解耦"
  - "分组视图不接入批量选择（selectMode），保持简单；DensityToggle 在分组视图下同样显示（卡片受 --card-w 密度影响）"

patterns-established:
  - "分组渲染：useMemo 按 brand 归组 + localeCompare('zh') 组间排序，组内保持传入顺序（不重排 server 排序结果）"

requirements-completed: [BRAND-GROUP-VIEW]

duration: 8min
completed: 2026-07-10
---

# Quick 260710-709: 网格视图按品牌分组展示 Summary

**库页面新增可切换、可持久化的「分组」视图模式：卡片按 brand（开发商）一组一组展示，每组品牌名标题 + 组内密度自适应卡片网格，null/空 brand 归「未知品牌」组并排末尾。**

## Performance

- **Duration:** ~8 min
- **Tasks:** 2 completed
- **Files created:** 1
- **Files modified:** 3

## Accomplishments

- **Task 1** — 新建 `GameGroupGrid.tsx`（145 行）：自包含封面解析（复刻 GameGrid 的 `get_data_dir` + resolveCover 闭包）、`useMemo` 按 `game.brand` 归组、`localeCompare("zh")` 组间升序 + 未知品牌恒定置底、组内 `repeat(auto-fill, minmax(var(--card-w), 1fr))` 密度自适应网格，复用 `GameCard` 全部回调（onRefreshCover/onMutated 归约到 onChildMutation）。
- **Task 2** — 接线视图模式：
  - `preferences.ts` — `ViewMode` 增加 `"group"` 成员 + `VIEW_MODES` 数组（`isViewMode` 校验器读 VIEW_MODES 自动兼容，持久化零改动）。
  - `ViewToggle.tsx` — `OPTIONS` 追加第三项 `{ value: "group", label: "分组", Icon: Boxes }`（lucide `Boxes` 图标，硬编码中文，不引 i18n）。
  - `Library.tsx` — import GameGroupGrid + DensityToggle 显示条件扩展到 group 视图 + list 分支后新增 `viewMode === "group"` 渲染分支（透传 visibleGames + 卡片回调）。

## Deviations from Plan

None - plan executed exactly as written.

（说明：plan 的 grep 自验期望 `grep -c 'viewMode === "group"' src/routes/Library.tsx` = 1，实测 = 2。这是符合预期的：plan 同时要求把 DensityToggle 显示条件改为 `(viewMode === "grid" || viewMode === "group")` 并新增 group 渲染分支，两处各引用一次。所有 must_have truths 均满足。）

## Verification

- `tsc --noEmit -p tsconfig.json` → EXIT 0
- `tsc -b && vite build` → EXIT 0（1989 modules transformed, built in 5.50s）
- grep 自验全中：preferences.ts `"group"` ×2 · ViewToggle `分组` ×1 · Library `viewMode === "group"` ×2 · Library `GameGroupGrid` ×2 · GameGroupGrid `未知品牌` ×4。
- i18n 未触碰（ViewToggle 保持硬编码中文），无 verify-i18n 键对齐 churn。

## GUI 真机验证（子代理跑不了 GUI，交付用户确认）

1. 工具栏视图切换器出现第三个「分组」按钮，点击后卡片按品牌分组、每组有品牌名标题 + mono 计数。
2. 组内卡片网格随密度切换（小/中/大）改变列宽。
3. 无 brand 的游戏聚在「未知品牌」组且排在最后。
4. 切到分组视图 → 重启应用 → 仍是分组视图（localStorage 持久化）。
5. 分组视图下卡片右键菜单（启动/元数据/删除/整理子目录）正常工作。

## Known Stubs

None.

## Commits

- `4d559c4` feat(quick-260710-709): add GameGroupGrid brand-grouped card grid
- `879b883` feat(quick-260710-709): wire group view mode into toggle + Library

## Self-Check: PASSED

- FOUND: src/components/library/GameGroupGrid.tsx
- FOUND: commit 4d559c4
- FOUND: commit 879b883
