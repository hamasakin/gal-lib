---
phase: quick-260516-uh6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/routes/Detail.tsx]
autonomous: true
requirements: [UH6-01]

must_haves:
  truths:
    - "打开一个可执行文件为 *_cn.exe（或 _chs/_zh/-cn/-chs/-zh）的游戏详情页，且该游戏没有保存过 le_profile 时，LE 默认配置为「简体中文」"
    - "打开一个日文版 EXE（无中文后缀）的游戏详情页时，LE 默认配置仍为「Japanese」"
    - "已显式保存过 le_profile 的游戏，详情页仍按保存值显示，不被 cn 默认覆盖"
    - "executable_path 为 null 时不报错，默认回落到「Japanese」"
  artifacts:
    - path: "src/routes/Detail.tsx"
      provides: "isCnVersionExe 纯函数 + refreshGame else 分支的 cn 默认逻辑"
      contains: "isCnVersionExe"
  key_links:
    - from: "src/routes/Detail.tsx refreshGame"
      to: "isCnVersionExe(g.executable_path)"
      via: "else 分支选择默认 profile"
      pattern: "isCnVersionExe"
---

<objective>
当游戏的可执行文件是中文版 EXE（文件名 stem 以中文补丁后缀结尾，如 `Game_cn.exe`）时，详情页的 LE 启动配置默认值应为「简体中文」而非「Japanese」。

Purpose: 中文版 EXE 用日文 LE 转区启动通常会乱码或行为异常；玩家明确装了中文补丁，默认就该用中文 LE，省去每次手动切换。
Output: `src/routes/Detail.tsx` 中新增纯函数 `isCnVersionExe` 并修改 `refreshGame` 的默认 profile 分支。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/routes/Detail.tsx

<interfaces>
<!-- 执行器需要的关键契约，已从代码库提取 — 无需再探索代码库 -->

src/routes/Detail.tsx 现有定义（line 160-166）:
```typescript
const LE_PROFILES = [
  "Japanese",
  "Simplified Chinese",
  "Traditional Chinese",
  "Custom",
] as const;
type LeProfile = (typeof LE_PROFILES)[number];
```
注意：`"Simplified Chinese"` 是 `LeProfile` 的合法成员。

src/routes/Detail.tsx 现有 refreshGame else 分支（line 472-476，待修改）:
```typescript
const x = g as Game & LaunchExtras;
const p = (LE_PROFILES as readonly string[]).includes(x.le_profile ?? "")
  ? (x.le_profile as LeProfile)
  : "Japanese";
setProfile(p);
```
`profile` state 同时驱动 hero 区 `LaunchButton` 弹层与「启动配置」tab 的 LE Profile Select，改一处 state 即覆盖两个界面。

锁定的检测约定 — src-tauri/src/scan/exe_score.rs line 76（必须复用同一组后缀，不要自创）:
```rust
let cn_suffixes = ["_cn", "_chs", "_zh", "-cn", "-chs", "-zh"];
```
Rust 侧对 `path.file_stem()`（去掉 `.exe` 的文件名）做匹配。六个后缀全部对应简体中文。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 isCnVersionExe 纯函数并接入 refreshGame 默认 profile 逻辑</name>
  <files>src/routes/Detail.tsx</files>
  <behavior>
    isCnVersionExe(path) 的预期行为（无 FE 单测框架，以下为执行器自检清单）:
    - isCnVersionExe("C:/games/Fate/Fate_cn.exe") === true
    - isCnVersionExe("C:/games/Fate/Fate_CHS.exe") === true（大小写不敏感）
    - isCnVersionExe("D:/x/game-zh.exe") === true
    - isCnVersionExe("C:/games/Fate/Fate.exe") === false（无后缀）
    - isCnVersionExe("C:/games/cncompany/Fate.exe") === false（仅目录含 cn，stem 不以后缀结尾）
    - isCnVersionExe("") === false / isCnVersionExe(null) === false（空/缺失不报错）
    refreshGame 的预期行为:
    - 游戏已保存合法 le_profile → 仍用保存值（不受本改动影响）
    - 未保存 le_profile + executable_path 是 *_cn.exe → profile 默认 "Simplified Chinese"
    - 未保存 le_profile + 普通日文 EXE → profile 默认 "Japanese"
    - 未保存 le_profile + executable_path 为 null → profile 默认 "Japanese"
  </behavior>
  <action>
1) 在 `src/routes/Detail.tsx` 顶部（紧跟 `LE_PROFILES`/`LeProfile` 定义之后，约 line 166 后）新增一个模块级纯函数：

```typescript
/**
 * 判断一个可执行文件路径是否为中文补丁版 EXE。
 * 后缀集合与后端 src-tauri/src/scan/exe_score.rs 的 `cn_suffixes`
 * 保持一致（六个后缀，全部对应简体中文）。两处因跨语言无法共享，
 * 修改任一处务必同步另一处。
 */
function isCnVersionExe(path: string | null | undefined): boolean {
  if (!path) return false;
  // 取文件名 stem：去掉路径分隔符（兼容 \ 与 /）与扩展名
  const base = path.split(/[\\/]/).pop() ?? "";
  const stem = base.replace(/\.[^.]*$/, "").toLowerCase();
  const cnSuffixes = ["_cn", "_chs", "_zh", "-cn", "-chs", "-zh"];
  return cnSuffixes.some((suf) => stem.endsWith(suf));
}
```

2) 修改 `refreshGame`（约 line 472-476）的默认 profile 分支。**只改 else 分支**——保存了合法 `le_profile` 的分支保持不变：

```typescript
const x = g as Game & LaunchExtras;
const saved = x.le_profile ?? "";
const p: LeProfile = (LE_PROFILES as readonly string[]).includes(saved)
  ? (saved as LeProfile)
  : isCnVersionExe(g.executable_path)
    ? "Simplified Chinese"
    : "Japanese";
setProfile(p);
```

约束：
- 不改任何后端/Rust 文件。
- 不改 `LaunchButton.tsx`。
- 不引入新依赖，不新建文件——`isCnVersionExe` 就放在 Detail.tsx 内。
- 检测输入用 `g.executable_path`（Detail 加载时已是 `_cn.exe`），不要用其它字段。
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <done>`pnpm tsc --noEmit` 通过；Detail.tsx 含 `isCnVersionExe` 函数且带交叉引用 exe_score.rs 的注释；refreshGame 的 else 分支对中文 EXE 默认 "Simplified Chinese"、其余默认 "Japanese"，保存了 le_profile 的分支未改动。</done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` 无类型错误。
- 手动验证（无 FE 单测，需开发者过目）：
  1. 打开一个可执行文件为 `*_cn.exe` 且从未改过 LE 配置的游戏详情页 → hero 启动按钮弹层与「启动配置」tab 的 LE Profile 均默认「简体中文」。
  2. 打开一个普通日文版 EXE 的游戏详情页 → LE 默认仍为「Japanese」。
  3. 找一个已手动保存过 LE 配置（非简中）的游戏 → 详情页仍显示其保存值，未被 cn 默认覆盖。
</verification>

<success_criteria>
- 中文版 EXE 且无保存配置的游戏，详情页 LE 默认「简体中文」。
- 日文版 EXE 默认仍为「Japanese」。
- 已保存 `le_profile` 的游戏行为不变。
- `executable_path` 为 null 不报错，回落「Japanese」。
- 仅改动 `src/routes/Detail.tsx`，无后端改动。
</success_criteria>

<output>
完成后创建 `.planning/quick/260516-uh6-cn-exe-exe-le/260516-uh6-SUMMARY.md`
</output>
