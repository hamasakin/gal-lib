---
quick_id: 20260512
slug: cover-cache-buster
date: 2026-05-12
status: complete
description: 封面 cache-buster — bind/refresh 元数据后前端立即看到新封面
---

# Quick Task 20260512 — 封面 cache-buster

## 完成

4 处 `convertFileSrc(coverPath)` 全部加上 `?v=${encodeURIComponent(last_scanned_at ?? "")}`。

| 文件 | 改动 |
|---|---|
| `src/routes/Detail.tsx:578` | coverSrc 追加 `?v=` |
| `src/components/library/GameGrid.tsx:85-86` | resolveCover 追加 `?v=` |
| `src/components/library/ActiveSessionBar.tsx:84` | coverSrc 追加 `?v=` |
| `src/routes/Stats.tsx:660,667` | TopRow Pick 加 `last_scanned_at` + coverSrc 追加 `?v=` |

## 行为

- `bind_metadata` / `refresh_metadata` 写 `last_scanned_at = datetime('now')`（已有逻辑，未改后端）
- 前端 `<img src>` 因为 `?v=` 变化 → WebView image cache 不再命中 → 立刻显示新封面
- cover_path 为 NULL 时（placeholder 阶段或无封面）整个 src 仍是 null，行为不变

## 验证

- `pnpm tsc --noEmit` 通过（EXIT=0）
- 手动 E2E 待用户实测（在 Detail 页 bind 一次元数据，看封面是否切换）

## 不动

- 后端 `cover_cache.rs` 文件名规则未改
- "重新抓取封面" vs "重新匹配元数据" 的语义合并问题（之前对话讨论的 A/B/C）未在本次范围

## Files changed

- src/routes/Detail.tsx
- src/components/library/GameGrid.tsx
- src/components/library/ActiveSessionBar.tsx
- src/routes/Stats.tsx
