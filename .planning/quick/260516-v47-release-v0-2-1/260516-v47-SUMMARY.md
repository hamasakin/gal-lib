---
phase: quick-260516-v47
plan: 01
subsystem: build/release
tags: [release, versioning, chore]
requires: []
provides: [v47-RELEASE-021]
affects: [package.json, src-tauri/tauri.conf.json, src-tauri/Cargo.toml, src-tauri/Cargo.lock]
tech-stack:
  added: []
  patterns: ["三处版本字段 + Cargo.lock 自身条目必须保持一致"]
key-files:
  created: []
  modified:
    - package.json
    - src-tauri/tauri.conf.json
    - src-tauri/Cargo.toml
    - src-tauri/Cargo.lock
decisions:
  - "Cargo.lock 仅修改 gal-lib 自身 [[package]] 条目的 version，未运行 cargo update（workspace 自身包版本变化不影响依赖解析，无需联网）"
metrics:
  duration: "~3 min"
  completed: "2026-05-16"
  tasks: 1
  files: 4
---

# Quick 260516-v47: 发布 v0.2.1 小版本（版本号 bump）Summary

将 gal-lib 版本号从 0.2.0 统一升级到 0.2.1，组成单个原子提交，为 v0.2.1 发布做准备。

## What Was Done

将 4 处版本字段从 `0.2.0` 升到 `0.2.1`：

| 文件 | 字段 | 变更 |
|------|------|------|
| `package.json` | `"version"` (L4) | `0.2.0` → `0.2.1` |
| `src-tauri/tauri.conf.json` | `"version"` (L4) | `0.2.0` → `0.2.1` |
| `src-tauri/Cargo.toml` | `version` (L3) | `0.2.0` → `0.2.1` |
| `src-tauri/Cargo.lock` | `gal-lib` 包条目 `version` (L1573) | `0.2.0` → `0.2.1` |

`git diff --stat` 确认恰好 4 个文件被修改，每个文件仅一行变更（4 insertions / 4 deletions）。Cargo.lock 中其余所有依赖条目未改动，仅 `name = "gal-lib"` 的 `[[package]]` 条目 version 更新。

## Verification

- `git diff` 显示 4 个文件各 1 行变更（`0.2.0` → `0.2.1`），无其他改动。
- `git grep '0\.2\.0' -- package.json src-tauri/tauri.conf.json src-tauri/Cargo.toml` 无输出（三处用户/构建版本字段已无 0.2.0 残留）。
- Cargo.lock 第 1571-1573 行：`[[package]]` / `name = "gal-lib"` / `version = "0.2.1"` —— 与 Cargo.toml 一致。
- 提交后 deletion check 无文件删除，无未跟踪文件残留。

## Commits

- `8edf43e` — chore(quick-260516-v47): bump version to 0.2.1

## Deviations from Plan

None - plan executed exactly as written.

### 执行环境说明（非偏离，仅记录）

执行起始时 worktree merge-base 与目标 base (`6e50931`) 不一致，按 `<worktree_branch_check>` 流程 `git reset --hard 6e50931` 校正了 base。校正后通过 Read 工具读到的文件内容曾出现与磁盘 (`od` 字节级校验) 不一致的状态，遂改用 `perl -i` 行定位编辑直接落盘，并以 `git diff` 字节级确认改动生效。最终提交内容与计划完全一致。

## Self-Check: PASSED

- 提交存在：`8edf43e` 在 `git log` 中可查。
- 修改文件确认：`git show 8edf43e --stat` 显示 package.json / src-tauri/tauri.conf.json / src-tauri/Cargo.toml / src-tauri/Cargo.lock 共 4 个文件，每个 1 行 `0.2.0`→`0.2.1`。
- 未执行 `git tag` / `git push`（按约束由 orchestrator 处理）。
- 未修改 ROADMAP.md，未提交 docs 工件（由 orchestrator 处理）。
