---
phase: quick-260710-jk9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/commands.rs
  - src-tauri/src/lib.rs
  - src/lib/games.ts
  - src/components/library/EditGameInfoDialog.tsx
  - src/routes/Detail.tsx
  - src/locales/zh-CN/translation.json
  - src/locales/ja-JP/translation.json
  - src/locales/en-US/translation.json
autonomous: true
requirements: [QUICK-260710-jk9]
must_haves:
  truths:
    - "用户能在游戏详情页「更多」菜单打开「编辑条目信息」对话框"
    - "用户能修改条目名称并保存，详情页标题（h1）立即更新"
    - "用户能从已有品牌列表中选择所属品牌（下拉选择，无法自由输入新品牌），保存后品牌立即更新"
    - "手动编辑的条目名称在「刷新元数据」（refresh_metadata_smart）后仍保留"
  artifacts:
    - path: "src-tauri/src/commands.rs"
      provides: "update_game_title 命令 — 写 name_cn（显示标题字段）"
      contains: "pub async fn update_game_title"
    - path: "src-tauri/src/lib.rs"
      provides: "update_game_title 注册进 invoke_handler"
      contains: "commands::update_game_title"
    - path: "src/lib/games.ts"
      provides: "updateGameTitle 前端封装"
      contains: "export async function updateGameTitle"
    - path: "src/components/library/EditGameInfoDialog.tsx"
      provides: "标题输入 + 品牌下拉（仅列已有品牌）对话框"
      min_lines: 80
    - path: "src/routes/Detail.tsx"
      provides: "「编辑条目信息」菜单入口 + 对话框接线 + 保存处理"
      contains: "EditGameInfoDialog"
  key_links:
    - from: "src/routes/Detail.tsx"
      to: "EditGameInfoDialog"
      via: "DropdownMenuItem onSelect 打开对话框"
      pattern: "EditGameInfoDialog"
    - from: "src/routes/Detail.tsx"
      to: "update_game_title / update_game_brand_year"
      via: "onSubmit -> updateGameTitle + updateGameBrandYear + refreshGame"
      pattern: "updateGameTitle"
    - from: "EditGameInfoDialog"
      to: "get_filter_options().brands"
      via: "品牌下拉选项源自已有品牌列表"
      pattern: "brands"
---

<objective>
游戏详情页支持手动编辑「条目名称」和「所属品牌」：名称为自由文本输入；品牌必须从库中已有品牌列表里下拉选择（不允许自由输入新品牌），并可选「无品牌」清除。

Purpose: 让用户修正元数据抓取错误或不满意的标题/厂商归属，且品牌收敛到已有取值避免拼写分叉产生重复品牌桶。
Output: 一个新后端命令 update_game_title + 一个前端编辑对话框，接入 Detail「更多」菜单。品牌复用现有 update_game_brand_year 命令。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- 关键契约，直接使用，无需再探索代码库 -->

标题显示回退规则（src/lib/display.ts）:
```typescript
export function displayGameName(
  game: Pick<Game, "name" | "name_cn" | "metadata_source" | "path">,
): string;
// 优先级: name_cn（非空即胜） -> 若 bound(bangumi/vndb/manual) 用 name -> 目录 basename
```
含义: 绝大多数条目要么有 name_cn（中文优先特性 260710-8le）、要么已绑定，可见标题由 **name_cn** 主导。因此「改标题」必须写 name_cn 才能让 h1 真正变化。

现有品牌命令（复用，勿改签名）:
```rust
// src-tauri/src/commands.rs:3304
pub async fn update_game_brand_year(game_id: i64, brand: Option<String>, release_year: Option<i32>, ...)
// UPDATE games SET brand = ?, release_year = ?, updated_at=... WHERE id = ?
// 注意: 二者都是「覆盖」语义，传 None 即清空。编辑品牌时须把当前 release_year 一并回传以免误清年份。
```
```typescript
// src/lib/games.ts:186
export async function updateGameBrandYear(gameId: number, brand: string|null, releaseYear: number|null): Promise<void>;
```

已有品牌列表来源:
```typescript
// src/lib/persons.ts — getFilterOptions()
export interface FilterOptions { brands: Array<{ name: string; count: number }>; /* ... */ }
export async function getFilterOptions(): Promise<FilterOptions>;
// 后端 get_filter_options: SELECT brand ... WHERE brand IS NOT NULL AND brand<>'' GROUP BY brand ORDER BY cnt DESC
```

刷新元数据的 name/name_cn 保全性（已核实）:
- refresh_metadata_smart（Settings「刷新元数据」/ commands.rs:2002 的 UPDATE）**不触碰 name/name_cn/brand 非COALESCE 覆盖** → 手动改的标题在刷新后保留。brand 用 COALESCE(?, brand)，源端返回品牌时会覆盖，返回 NULL 时保留。
- bind_metadata（「重新匹配元数据」/ commands.rs:1547）会无条件覆盖 name/name_cn —— 这是用户显式重选匹配，属预期，不在本任务范围内改动。

对话框样式参照物: src/components/library/ViewNameDialog.tsx（Dialog/DialogContent/DialogHeader/DialogFooter + 受控 open + onSubmit 返回 Promise 后再关闭）。
Select 原语: `@/components/ui/select` 导出 Select / SelectTrigger / SelectValue / SelectContent / SelectItem。
Detail「更多」菜单: src/routes/Detail.tsx:1180-1241，菜单项一律用 Radix `onSelect`（勿用 onClick，见该处 260519-21s 注释）。refreshGame() 回调（Detail.tsx:493）在 mutation 后重载 game。
i18n: src/locales/{zh-CN,ja-JP,en-US}/translation.json，扁平点号 key（如 "detail.menu.split_subdirs"），三语必须 key 数对齐。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 update_game_title 后端命令并注册</name>
  <files>src-tauri/src/commands.rs, src-tauri/src/lib.rs</files>
  <action>
在 commands.rs 的 update_game_notes / update_game_brand_year 附近新增一个命令：

```rust
/// 手动编辑条目「显示标题」。写入 `name_cn`（displayGameName 中最高优先、
/// 无需 metadata_source 参与就能生效的字段），因此绝大多数条目（含中文优先
/// name_cn 已填、或已绑定 bangumi/vndb）改标题都能让详情页 h1 立即变化。
///
/// 用 `NULLIF(?, name)`：当用户输入恰好等于底层 `name` 时 name_cn 落回 NULL，
/// 避免详情页出现「主标题 + 与之相同的副标题(altName)」重复。
///
/// 保全性：refresh_metadata_smart（「刷新元数据」）不触碰 name_cn，手动标题
/// 刷新后保留；bind_metadata（「重新匹配」）会覆盖，属用户显式重选匹配的预期。
#[tauri::command]
pub async fn update_game_title(
    game_id: i64,
    title: String,
    state: State<'_, AppPaths>,
) -> Result<(), String> {
    let trimmed = title.trim();
    if trimmed.is_empty() {
        return Err("条目名称不能为空".to_string());
    }
    let pool = state.pool().await.map_err(err_str)?;
    sqlx::query(
        "UPDATE games SET name_cn = NULLIF(?, name), updated_at = datetime('now') WHERE id = ?",
    )
    .bind(trimmed)
    .bind(game_id)
    .execute(&*pool)
    .await
    .map_err(err_str)?;
    Ok(())
}
```

注意: State 类型 / err_str / pool() 用法照抄同文件的 update_game_notes（同签名同错误风格），确认 `AppPaths` 与 `State` 的实际引入名一致（沿用现有 use）。

在 src-tauri/src/lib.rs 的 invoke_handler 列表中，在 `commands::update_game_brand_year,` 附近追加一行 `commands::update_game_title,`。
  </action>
  <verify>
    <automated>cd src-tauri && cargo check</automated>
  </verify>
  <done>cargo check 通过；grep 确认 lib.rs 出现 `commands::update_game_title` 恰 1 次、commands.rs 出现 `pub async fn update_game_title` 恰 1 次。</done>
</task>

<task type="auto">
  <name>Task 2: 编辑对话框（标题输入 + 品牌下拉）+ Detail 菜单接入 + i18n</name>
  <files>src/lib/games.ts, src/components/library/EditGameInfoDialog.tsx, src/routes/Detail.tsx, src/locales/zh-CN/translation.json, src/locales/ja-JP/translation.json, src/locales/en-US/translation.json</files>
  <action>
**(a) src/lib/games.ts** — 在 updateGameNotes / updateGameBrandYear 附近新增封装：
```typescript
/**
 * 手动设置条目「显示标题」。后端写入 name_cn（displayGameName 首选字段）。
 * 传入应为已 trim 的非空字符串；空串后端返回错误。
 */
export async function updateGameTitle(gameId: number, title: string): Promise<void> {
  await invoke("update_game_title", { gameId, title });
}
```

**(b) 新建 src/components/library/EditGameInfoDialog.tsx** — 参照 ViewNameDialog 结构（受控 open、Promise onSubmit 后再关闭、忙碌禁用、Enter 提交、共用 Dialog* 原语与配色）。要点：
- Props:
  ```typescript
  interface EditGameInfoDialogProps {
    open: boolean;
    initialTitle: string;          // 传 displayGameName(game) 结果，作为标题输入初值
    initialBrand: string | null;   // game.brand
    brands: Array<{ name: string; count: number }>; // 已有品牌列表（含计数）
    onClose: () => void;
    onSubmit: (title: string, brand: string | null) => Promise<void> | void;
  }
  ```
- 标题：文本 input（复用 ViewNameDialog 的 input 视觉与 trim/非空校验；空则禁用保存）。每次 open 用 useEffect 重置为 initialTitle 并 focus+select。
- 品牌：用 `@/components/ui/select` 的 Select 渲染下拉，**只列出 brands 里的项 + 一个「无品牌」哨兵项**——纯下拉选择，杜绝自由输入新品牌（满足「品牌必须从已有品牌列表中选择」）。
  - 用一个不会与真实品牌名冲突的哨兵值（如 `const NONE = "__none__";`）表示「无品牌」；提交时映射回 `null`。
  - 初值：initialBrand ?? NONE。若 initialBrand 非空但不在 brands 列表里（理论上不会，但兜底），把它作为额外一项插入，避免选中值不可见。
  - 每个 SelectItem 右侧可显示 count（可选，视觉参照 SortSelect / FilterPanel 品牌 facet）。
- Footer：取消（common.cancel）+ 保存。保存 disabled 条件：忙碌 || 标题 trim 为空 ||（标题与品牌均未变化）。onSubmit 传 (trimmedTitle, selectedBrand===NONE ? null : selectedBrand)。
- 所有可见文案走 useTranslation() t()。

**(c) src/routes/Detail.tsx** 接线：
- import `EditGameInfoDialog` 与 `updateGameTitle`（games.ts）、`getFilterOptions`（persons.ts，若已引入则复用）、Pencil 图标（lucide-react）。
- 新增 state：`const [editOpen, setEditOpen] = useState(false);` 和 `const [brandOptions, setBrandOptions] = useState<Array<{name:string;count:number}>>([]);`
- 在「更多」DropdownMenuContent 内（split_subdirs 附近，合适分组处）加一项：
  ```tsx
  <DropdownMenuItem onSelect={() => { void getFilterOptions().then((o) => setBrandOptions(o.brands)).catch(() => setBrandOptions([])); setEditOpen(true); }}>
    <Pencil size={14} className="mr-2" />
    {t("detail.menu.edit_info")}
  </DropdownMenuItem>
  ```
  （用 onSelect，勿 onClick；参见该文件 260519-21s 注释。）
- 在 JSX 末尾（SubdirSplitDialog 附近）渲染：
  ```tsx
  <EditGameInfoDialog
    open={editOpen}
    initialTitle={displayName}
    initialBrand={game.brand}
    brands={brandOptions}
    onClose={() => setEditOpen(false)}
    onSubmit={async (title, brand) => {
      try {
        await updateGameTitle(game.id, title);
        await updateGameBrandYear(game.id, brand, game.release_year); // 回传当前年份，避免误清
        await refreshGame();
        toast.success(t("detail.edit.saved"));
      } catch (e) {
        console.error("[Detail] edit info failed:", e);
        toast.error(t("detail.edit.failed"));
      }
    }}
  />
  ```
  （`displayName`、`game`、`refreshGame`、`updateGameBrandYear`、`toast` 均已在 Detail 作用域内；updateGameBrandYear 已 import。）

**(d) i18n** — 三语文件各新增同一组 key（值按语言翻译），保持 key 数对齐：
- `detail.menu.edit_info`：编辑条目信息 / 項目情報を編集 / Edit entry
- `detail.edit.title`：编辑条目信息 / 項目情報を編集 / Edit entry
- `detail.edit.title_label`：条目名称 / タイトル / Title
- `detail.edit.brand_label`：所属品牌 / ブランド / Brand
- `detail.edit.brand_none`：无品牌 / ブランドなし / No brand
- `detail.edit.brand_hint`：只能从库中已有品牌选择 / 既存ブランドから選択 / Choose from existing brands only
- `detail.edit.saved`：已保存 / 保存しました / Saved
- `detail.edit.failed`：保存失败 / 保存に失敗しました / Failed to save
（对话框内如需标题输入 placeholder / 副标题等自行补 key，三语同步。）
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm build</automated>
  </verify>
  <done>tsc --noEmit 与 vite build 全绿；三语 translation.json key 数相等（可用 node 快速点数：三文件 Object.keys 长度一致）；EditGameInfoDialog 品牌控件为下拉 Select（无自由文本 input）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React 前端 → Tauri IPC (invoke) | 用户输入的标题/品牌经 IPC 进入 Rust；此为本地单机应用，唯一不可信输入即用户自填文本 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-jk9-01 | Tampering | update_game_title 的 title 参数 | mitigate | sqlx 参数化绑定（`.bind`），无字符串拼接 SQL，杜绝注入；空标题服务端 trim+拒绝 |
| T-jk9-02 | Tampering | 品牌自由输入产生新品牌桶/拼写分叉 | mitigate | 前端品牌控件为纯下拉 Select，选项仅来自 get_filter_options().brands + 「无品牌」哨兵，无自由文本入口 |
| T-jk9-03 | Denial of Service | 超长标题 | accept | 本地单用户、单行 UPDATE，无放大面；前端可选加 maxLength 软限制，不作硬阻断 |
</threat_model>

<verification>
- `cargo check`（Task 1）通过；`pnpm exec tsc --noEmit` + `pnpm build`（Task 2）通过。
- grep：lib.rs `commands::update_game_title` ×1；commands.rs `pub async fn update_game_title` ×1；Detail.tsx `EditGameInfoDialog` 出现（import + JSX）。
- 三语 translation.json key 数对齐（无缺漏）。
</verification>

<success_criteria>
- 详情页「更多」菜单出现「编辑条目信息」，打开对话框可改标题（自由文本）与品牌（下拉，仅已有品牌 + 无品牌）。
- 保存后 refreshGame 使 h1 标题与信息栏品牌立即更新。
- 品牌控件无法自由输入新品牌值。
- 手动标题写入 name_cn，「刷新元数据」后仍在（因 refresh_metadata_smart 不触碰 name_cn）。
- 品牌编辑复用 update_game_brand_year 并回传当前 release_year，不误清年份。
- 自动化门（cargo check / tsc / build）全绿。
- GUI 真机验证（菜单入口 / 改标题即时生效 / 品牌下拉仅列已有 / 刷新元数据后标题保留）待用户在新 build 出来后确认。
</success_criteria>

<output>
完成后创建 `.planning/quick/260710-jk9-edit-title-brand/260710-jk9-SUMMARY.md`
</output>
