---
quick_id: 260525-tw2
slug: register-v13-migration
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/db.rs
autonomous: true
requirements:
  - QUICK-260525-tw2
tags:
  - bugfix
  - migration
  - tauri-plugin-sql
  - schema
must_haves:
  truths:
    - "tauri-plugin-sql 启动时会执行 0013_add_external_rating.sql，把旧库 schema_version 从 12 升到 13"
    - "games 表存在 external_rating / external_rating_count / external_rating_source 三列 + idx_games_external_rating 索引"
    - "search_games / list_games / get_game / list_games_for_person 四处 SELECT external_rating 不再因列缺失报错"
    - "前端 Library 启动后能正常拿到 games 列表（不再是 silent-swallow 后的空数组）"
  artifacts:
    - path: "src-tauri/src/db.rs"
      provides: "v13 Migration 入口（const V13_SQL + migrations() vec 末尾追加 v13 entry + 顶部 module-doc 补一行）"
      contains: "version: 13"
  key_links:
    - from: "src-tauri/src/db.rs::migrations()"
      to: "src-tauri/migrations/0013_add_external_rating.sql"
      via: "include_str! const V13_SQL"
      pattern: "0013_add_external_rating\\.sql"
    - from: "src-tauri/src/db.rs::migrations() vec"
      to: "tauri-plugin-sql 启动流"
      via: "Migration { version: 13, ... }"
      pattern: "version: 13"
---

<objective>
修复 Quick `260525-g1m`（官方评分入库 + 排序升降序切换）遗留的 migration 漏注册 BUG。

`src-tauri/migrations/0013_add_external_rating.sql` 文件存在、SQL 正确，但 `src-tauri/src/db.rs::migrations()` 的 vec 里没有 v13 entry —— tauri-plugin-sql 只跑 vec 里登记过的 migration，所以旧库 schema_version 卡在 12，games 表缺 `external_rating` / `external_rating_count` / `external_rating_source` 三列。

由此引发的级联崩坏：
1. `search_games` SELECT 失败 → 返回 Err
2. 前端 `src/routes/Library.tsx:340-343` 在 catch 里只 console.error、不清空 store.games → 初始 `[]` 永远没被填充
3. 用户看到「条目数正常但游戏列表全空」

Purpose: 一处单文件改动恢复 v12 → v13 升级路径；上一轮 Quick 的所有上游改动（SQL 文件、commands.rs SELECT、ORDER BY）都已经正确，缺的只是这一行注册。

Output: `src-tauri/src/db.rs` 的 3 处定向追加（module-doc 一行 + const V13_SQL + migrations() vec 末尾 Migration entry）。

不做：
- 不改 SQL 文件（已正确）
- 不改 `src-tauri/src/commands.rs`（已读取三列、ORDER BY 已切到 external_rating）
- 不改前端 catch silent-swallow（另一个独立任务）
- 不发版（v0.3.2 由主对话在用户验证 OK 后发）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src-tauri/src/db.rs
@src-tauri/migrations/0013_add_external_rating.sql

<interfaces>
<!-- v11/v12 是同型先例 —— 照抄它们的 ALTER + INDEX + UPDATE app_meta 三件套写法。 -->
<!-- db.rs 模块结构（已读过，直接对位插入）： -->

顶部 module-doc 现在最后一段（line 28-30）：
```
//! Schema v12 (Quick 260516-q3y) adds 1 new table (`scan_skip_dirs`) — a
//! persistent skip-list of brand parent directories split into per-game
//! subdir entries, so a full scan never re-discovers them as games.
```
→ 紧接其后追加 v13 段（4 行注释）。

const 块现在最后一行（line 45）：
```rust
const V12_SQL: &str = include_str!("../migrations/0012_add_scan_skip_dirs.sql");
```
→ 紧接其后追加 V13_SQL const（1 行）。

migrations() vec 现在最后一个 entry（line 118-123）：
```rust
        Migration {
            version: 12,
            description: "add_scan_skip_dirs",
            sql: V12_SQL,
            kind: MigrationKind::Up,
        },
```
→ 紧接其后（vec 闭合 `]` 之前）追加 v13 Migration entry（6 行）。

Migration 结构来自 `tauri_plugin_sql::{Migration, MigrationKind}`（line 32），字段固定为 version / description / sql / kind。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 db.rs 注册 v13 migration 并 cargo check 验证</name>
  <files>src-tauri/src/db.rs</files>
  <action>
对 `src-tauri/src/db.rs` 做 3 处定向追加（不要动其它任何代码、注释、空行）：

**(1) 顶部 module-doc 追加 v13 段**

在现有 v12 doc 段（`//! Schema v12 (Quick 260516-q3y) ...` 那 3 行）之后、`use tauri_plugin_sql::{Migration, MigrationKind};` 之前，追加：

```
//! Schema v13 (Quick 260525-g1m) adds 3 columns on `games`
//! (external_rating + external_rating_count + external_rating_source) and
//! index `idx_games_external_rating` —— 官方评分（Bangumi/VNDB）入库与排序锚点。
```

**(2) const 块末尾追加 V13_SQL**

在现有 `const V12_SQL: &str = include_str!("../migrations/0012_add_scan_skip_dirs.sql");` 之后追加一行：

```rust
const V13_SQL: &str = include_str!("../migrations/0013_add_external_rating.sql");
```

**(3) migrations() vec 末尾追加 v13 entry**

在 vec 现有最后一个 entry（v12，以 `},` 结尾）之后、`]` 之前追加：

```rust
        Migration {
            version: 13,
            description: "add_external_rating",
            sql: V13_SQL,
            kind: MigrationKind::Up,
        },
```

注意：
- `description` 字段值 `"add_external_rating"` —— 与 SQL 文件名后半段保持同型（v12 是 `"add_scan_skip_dirs"` 对应 `0012_add_scan_skip_dirs.sql`）
- 缩进与 v12 entry 完全一致（8 空格）
- 末尾 `,` 保留（vec 风格统一）
- 不要新增 #[test]（本次只补注册；如要补测试是另一个任务）

完成后立刻在 `D:/project/gal-lib/src-tauri` 目录跑 `cargo check`，必须通过（include_str! 路径正确 + Migration 结构字段匹配 + V13_SQL 被引用消除 dead_code 警告）。
  </action>
  <verify>
    <automated>cd src-tauri && cargo check 2>&1 | tail -20</automated>
    必须看到 `Finished ... profile [unoptimized + debuginfo] target(s)` 且无 error；warning 允许（关注 V13_SQL 不应出现 `unused` 警告）。
    另外用 grep 验证 3 处追加都到位：
    `grep -c "version: 13" src-tauri/src/db.rs` 应返回 ≥ 1
    `grep -c "V13_SQL" src-tauri/src/db.rs` 应返回 ≥ 2（const + Migration entry 引用各 1 次）
    `grep -c "0013_add_external_rating.sql" src-tauri/src/db.rs` 应返回 1
  </verify>
  <done>
- `src-tauri/src/db.rs` 含 `const V13_SQL` + `Migration { version: 13, description: "add_external_rating", sql: V13_SQL, kind: MigrationKind::Up }`
- 顶部 module-doc 含 v13 4 行注释段
- `cd src-tauri && cargo check` 通过（无 error）
- 文件其它部分（v1-v12 const、v1-v12 vec entry、所有 #[test] 块）完全未变
  </done>
</task>

</tasks>

<verification>
本任务为单文件 3 处定向追加 + cargo check，验证全在 task 1 内部完成。

不在本任务范围（由用户在 build 完成后真机执行）：
- 重启 v0.3.2 build，旧库自动跑 v13 migration
- Library 页恢复显示游戏列表（不再空）
- Settings「刷新元数据」回填 external_rating 后排序按官方评分生效
</verification>

<success_criteria>
- `src-tauri/src/db.rs` 3 处定向追加全部到位（grep 验证）
- `cd src-tauri && cargo check` 通过
- 文件 diff 应只在 module-doc 末段 / const 块末尾 / migrations() vec 末尾出现新增行，无其它修改
</success_criteria>

<output>
After completion, create `.planning/quick/260525-tw2-register-v13-migration/260525-tw2-SUMMARY.md`.

SUMMARY 必须明确写：
- 自动化验证（cargo check）已通过
- **GUI / 真机验证（重启应用 → 旧库 v12→v13 migration → games 列表恢复）由用户在新 build 出来后亲自确认**，本次任务不写「已根治」
- 后续动作（用户验证 OK 后由主对话执行）：发 v0.3.2 patch release（`npm run release patch`）
</output>
