---
quick_id: 20260512f
slug: sidebar-polish-and-view-dialogs
description: Sidebar 视觉打磨 + ViewNameDialog / DeleteViewDialog 替换 window.prompt/confirm
date: 2026-05-12
status: complete
mode: quick
---

# Quick 20260512f — Sidebar 打磨 + 视图对话框

## 改动文件

| 文件 | 类型 | 摘要 |
|------|------|------|
| `src/components/library/ViewNameDialog.tsx` | 新增 | 新建 / 重命名视图通用 Dialog |
| `src/components/library/DeleteViewDialog.tsx` | 新增 | 删除视图 AlertDialog |
| `src/components/layout/Sidebar.tsx` | 重写 | section + 行视觉重做，dialog 状态化，CustomViewRow 抽出 |
| `src/routes/Library.tsx` | 修改 | 批量新建视图改走 ViewNameDialog |

## 视觉契约要点

- **Section header**：印章红短线 + mono uppercase 9.5px tracking 0.16em
- **Row**：32px 高 / 14px 图标 / 2px brand 左缘 active 指示 / group hover 联动 ink-1 ink-2
- **Custom view 行**：hover 数字淡出 / ⋯ 按钮淡入；DropdownMenu open 时持续显示
- **空态**：dashed CTA "＋ 新建第一个视图" 取代灰色单行文案
- **Dialog**：印章方块 glyph + serif 标题 + mono 副标 + 字符计数 + brand/destructive 主操作

## 删除

- `window.prompt` × 2（Sidebar.onCreateView, Library.onCreateAndAdd）
- `window.confirm` × 1（Sidebar.onDeleteView）
- Sidebar 内联 rename input（onBlur 误提交隐患一并解决）
- Sidebar 对 ContextMenu 的依赖（DropdownMenu + ⋯ 显式触发取代）

## 验证

- `pnpm tsc --noEmit` exit 0
- `pnpm build` exit 0，bundle 739KB (gzip 222KB)
- 视觉验证：实机用户复核
