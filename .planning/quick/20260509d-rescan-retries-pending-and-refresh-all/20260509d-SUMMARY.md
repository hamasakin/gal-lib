---
quick_id: "20260509d-rescan-retries-pending-and-refresh-all"
slug: "rescan-retries-pending-and-refresh-all"
date: "2026-05-09"
status: complete
---

# Quick Task Summary: rescan-retries-pending-and-refresh-all

## What changed

| Commit | What |
|---|---|
| `0725c5a` | `fix(scan): incremental retries 「待复核」 rows; bound rows still skipped` |
| `fad2044` | `feat(metadata): refresh_all_metadata command + Settings 强制刷新 button` |

## Truths delivered (against PLAN.md must_haves)

- ✅ Incremental scan no longer skips `metadata_source='none'` rows.
  `start_scan`'s `existing_paths` SELECT now filters
  `WHERE metadata_source IN ('bangumi','vndb','manual')`.
- ✅ Manual binds remain skipped on incremental rescan — never
  silently overwritten.
- ✅ New `refresh_all_metadata` Tauri command iterates every game and
  re-runs `ingest::refresh_for_query`, updating each row in place.
  Manual-bound rows ARE refreshed on this path (gated by AlertDialog).
- ✅ Settings → 扫描操作 gains a third button 「强制刷新全部元数据」.
  Confirm dialog warns about overriding manual binds.
- ✅ Progress flows through the existing `scan-progress` event stream
  → ScanProgressBar Just Works for both flows.

## Verification

- `cargo build --lib` clean (4 pre-existing warnings unrelated)
- `pnpm typecheck` clean

## Manual verification needed under dev session

| Check | How |
|---|---|
| Incremental retry refreshes 待复核 rows | Library has a 「待复核」 card → 设置 → 增量扫描 → that card re-issues Bangumi+VNDB; bound cards do not |
| Bound rows untouched on incremental | A bound card's `name` and `match_confidence` unchanged after incremental rescan |
| Force-refresh button | 设置 → 扫描操作 → 强制刷新全部元数据 → 确定 → progress bar runs N/total → all rows get fresh `last_scanned_at` |
| Manual bind survives | A `metadata_source='manual'` row is skipped by incremental but IS reprocessed by force-refresh (and may be overwritten — that's the warning text's job) |

## Diff stats

```
src-tauri/src/commands.rs     | +137 / -8
src-tauri/src/lib.rs          |   +1 / -0
src/lib/scan.ts               |  +10 / -0
src/routes/Settings.tsx       |  +60 / -25
```
