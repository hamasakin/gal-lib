---
phase: quick-260513-3df
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/commands.rs
  - src-tauri/src/lib.rs
  - src/lib/scan.ts
  - src/lib/persons.ts
  - src/routes/Settings.tsx
autonomous: false
requirements:
  - QUICK-260513-3df
must_haves:
  truths:
    - "Settings「扫描操作」节只剩 2 个按钮：全量扫描 + 刷新元数据"
    - "「增量扫描」「补全发行年份」「强制刷新全部元数据」按钮 + AlertDialog 全部消失"
    - "点击「刷新元数据」对 DB 中已绑定行（含 manual）能按 source_id 直拉 detail/persons/characters，bangumi_id/vndb_id/metadata_source/name/name_cn 不被改"
    - "点击「刷新元数据」对未绑定行能走 ingest::refresh_for_query 模糊匹配并按现有 refresh_all_metadata 的 UPDATE shape 写回"
    - "新 IPC 共享 ScanState，cancel_scan 能中止；BackfillProgressBar 0 改动并能正常显示进度（meta-fetch-progress + scan-progress）"
    - "cargo check 通过、pnpm tsc --noEmit 通过"
  artifacts:
    - path: "src-tauri/src/commands.rs"
      provides: "新 IPC `refresh_metadata_smart`；删除 refresh_all_metadata + backfill_release_year"
      contains: "pub async fn refresh_metadata_smart"
      absent: "pub async fn refresh_all_metadata\npub async fn backfill_release_year"
    - path: "src-tauri/src/lib.rs"
      provides: "invoke_handler 注册新 IPC + 删两行旧注册"
      contains: "commands::refresh_metadata_smart"
      absent: "commands::refresh_all_metadata,\ncommands::backfill_release_year,"
    - path: "src/lib/scan.ts"
      provides: "refreshMetadataSmart 包装；refreshAllMetadata 包装删除"
      contains: "export async function refreshMetadataSmart"
      absent: "export async function refreshAllMetadata"
    - path: "src/lib/persons.ts"
      provides: "backfillReleaseYear 包装删除（cancelBackfill 保留）"
      absent: "export async function backfillReleaseYear"
    - path: "src/routes/Settings.tsx"
      provides: "scan-ops Section 改 2 按钮 + AlertDialog 去掉 + handler/import 清理"
      absent: "refreshAllMetadata\nbackfillReleaseYear\nonRefreshAllMetadata\nonBackfillReleaseYear\n增量扫描\n补全发行年份\n强制刷新全部元数据"
  key_links:
    - from: "src/routes/Settings.tsx"
      to: "refreshMetadataSmart in @/lib/scan"
      via: "import + button onClick"
      pattern: "refreshMetadataSmart"
    - from: "src/lib/scan.ts::refreshMetadataSmart"
      to: "Tauri command refresh_metadata_smart"
      via: "invoke"
      pattern: "invoke\\(\"refresh_metadata_smart\""
    - from: "src-tauri/src/commands.rs::refresh_metadata_smart"
      to: "ingest::refresh_for_query (未绑定) + metadata::{bangumi,vndb}::{fetch_detail,fetch_persons,fetch_characters} (已绑定)"
      via: "match on (bangumi_id, vndb_id, metadata_source)"
      pattern: "fetch_detail|refresh_for_query"
    - from: "src-tauri/src/commands.rs::refresh_metadata_smart"
      to: "write_staff_and_tags + scan-progress + meta-fetch-progress events"
      via: "复用现有事件协议 + helper"
      pattern: "write_staff_and_tags|scan-progress|meta-fetch-progress"
---

<objective>
把 Settings「扫描操作」节简化为 2 个按钮：① 全量扫描（行为不变，复用 `startScan("full")`） ② 刷新元数据（**新 IPC** `refresh_metadata_smart`：未绑定走模糊匹配、已绑定按 source_id 直拉 detail，对 manual 安全）。同时**彻底删除** `refresh_all_metadata`、`backfill_release_year`、「增量扫描」按钮、「补全发行年份」按钮、「强制刷新全部元数据」按钮 + 其 AlertDialog。

Purpose: 收敛认知负担——v1.3 之前 4 个按钮（全量/增量/补全年份/强制刷新）职责重叠且对 manual 不安全；本次合并为「发现新游戏」 vs 「刷新元数据」两个语义清晰的入口。对 manual 绑定行：bangumi_id/vndb_id/metadata_source/name 不变（手动标记 + 改过的名字保住）；summary/release_year 用户主动点了就直接覆盖（**注意：这里改变了 quick 260513-2nx 的 release_year COALESCE 策略**）；brand/age_rating/cover 仍走 COALESCE 防 manual 改过的丢失。

Output: 新 IPC `refresh_metadata_smart` + 前端按钮重排 + ~150 行 Rust + ~50 行 TS 删除。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260513-3df-two-button-metadata-refresh/

<interfaces>
<!-- 复用的现有契约 — 不要再 grep -->

From src-tauri/src/ingest.rs:
```rust
pub fn parse_release_year(s: Option<&str>) -> Option<i32>;
pub async fn refresh_for_query(
    game_id_for_cover: i64,
    data_dir: &Path,
    games_path: &str,
    query: &str,           // 用 games.name 做模糊匹配
    executable_path: Option<&str>,
) -> IngestResult;
```

From src-tauri/src/metadata/{bangumi,vndb}.rs:
```rust
pub async fn fetch_detail(source_id: &str) -> Result<MetadataDetail, MetadataError>;
pub async fn fetch_persons(source_id: &str) -> Result<Vec<PersonRef>, MetadataError>;
pub async fn fetch_characters(source_id: &str) -> Result<Vec<PersonRef>, MetadataError>;
```

From src-tauri/src/metadata/types.rs:
```rust
pub enum MetadataSource { Bangumi, Vndb, Manual, None }
pub struct MetadataDetail {
    pub source: MetadataSource, pub source_id: String,
    pub title: String, pub title_cn: Option<String>,
    pub cover_url: Option<String>, pub summary: Option<String>,
    pub release_date: Option<String>, pub brand: Option<String>,
    pub tags: Vec<OfficialTagRef>, pub is_r18: Option<bool>,
}
pub struct PersonRef { /* source, source_id, name, name_cn, role, character_name */ }
pub struct OfficialTagRef { pub name: String, pub weight: i32 }
```

From src-tauri/src/commands.rs (helpers):
```rust
async fn write_staff_and_tags(
    pool: &SqlitePool, game_id: i64, source_str: &str,
    result: &ingest::IngestResult,                  // 注意：写入靠 IngestResult.staff + .tags
) -> Result<(), String>;
```

From src-tauri/src/commands.rs (ScanState):
```rust
pub struct ScanState { pub ctx: Mutex<Option<Arc<scan::ScanContext>>> }
// 用法见 refresh_all_metadata:1188-1204 — 把 ctx clone 进闭包后 spawn
```
</interfaces>

<existing_update_shapes>
<!-- 已绑定行的目标 UPDATE（按用户决策合成） -->

未绑定行 (bangumi_id IS NULL AND vndb_id IS NULL)：
  完全复用 refresh_all_metadata 现有 UPDATE（commands.rs:1301-1328）。

已绑定行 (bangumi_id IS NOT NULL OR vndb_id IS NOT NULL)：
  - **不动**：bangumi_id, vndb_id, metadata_source, match_confidence, name, name_cn
  - **直接覆盖**：summary, release_year
  - **COALESCE 保留旧值**：brand, age_rating, cover_path, cover_url
  - last_scanned_at = datetime('now')

SQL 草稿（已绑定行）：
```sql
UPDATE games SET
  cover_path = COALESCE(?, cover_path),
  cover_url  = COALESCE(?, cover_url),
  summary    = ?,
  brand      = COALESCE(?, brand),
  age_rating = COALESCE(?, age_rating),
  release_year = ?,
  last_scanned_at = datetime('now')
WHERE id = ?
```
</existing_update_shapes>

<event_protocol>
<!-- 复用，不要造新事件 -->
- 进度条总体：`scan-progress` (ScanProgress { current_dir, completed, total, status })
- 单卡片脉冲：`meta-fetch-progress` { game_id, phase: "started"|"finished" }
- BackfillProgressBar.tsx 现在订阅 `meta-fetch-progress-meta` + `meta-fetch-progress`（不订阅 scan-progress）—— 但仍然有效，因为我们沿用相同的 per-game meta-fetch-progress 事件。整体进度由 scan-progress 推动 ScanProgressBar，与 BackfillProgressBar 互补。
- 取消：与 refresh_all_metadata 一致，共享 ScanState；cancel_scan 触发 ctx.cancel
</event_protocol>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 后端 — 新增 refresh_metadata_smart IPC + 删除两个旧函数 + 更新注册</name>
  <files>src-tauri/src/commands.rs, src-tauri/src/lib.rs</files>
  <action>
**A. 在 commands.rs 添加新 IPC `refresh_metadata_smart`（建议放在 refresh_all_metadata 原位置，1188 行附近，函数文档+签名为：）**

```rust
/// Quick 260513-3df — 两按钮统一刷新入口。
///
/// SELECT id, name, path, executable_path, bangumi_id, vndb_id, metadata_source
/// FROM games ORDER BY id ASC；对每行：
///   • 未绑定 (bangumi_id IS NULL AND vndb_id IS NULL) — 走 ingest::refresh_for_query
///     做模糊匹配，UPDATE shape 沿用旧 refresh_all_metadata（含 COALESCE 保护）。
///   • 已绑定 (任一非空) — 按 metadata_source 选源（manual/None 用「bangumi 优先 vndb fallback」），
///     直接调 fetch_detail + fetch_persons + fetch_characters，**不重做模糊匹配**。
///     UPDATE 仅写：cover_path/cover_url (COALESCE)、summary (覆盖)、brand (COALESCE)、
///     age_rating (COALESCE)、release_year (覆盖)、last_scanned_at；
///     **不动** bangumi_id / vndb_id / metadata_source / match_confidence / name / name_cn
///     —— 这保住 manual 标记和用户改过的名字，符合「主动点刷新」预期。
///   • staff/tags 通过 write_staff_and_tags 事务式重写（与旧 refresh_all_metadata 一致）。
///
/// 共享 ScanState 使 cancel_scan 可中止；进度走 scan-progress + meta-fetch-progress。
#[tauri::command]
pub async fn refresh_metadata_smart(
    app: AppHandle,
    state: State<'_, AppPaths>,
    scan_state: State<'_, ScanState>,
) -> Result<(), String> {
    // 1) 与 refresh_all_metadata:1193-1204 完全一致：取 pool / data_dir / 注入 ScanContext
    // 2) SELECT id, name, path, executable_path, bangumi_id, vndb_id, metadata_source
    //    FROM games ORDER BY id ASC
    // 3) 初始 scan-progress (Running, 0/total)；total==0 直接 Completed 返回
    // 4) tokio::spawn(async move { for (i, row) ... })
    //    每轮：
    //      a) cancel 检查 → 发 Cancelled 并 return
    //      b) 取 id / path / name / exec / bangumi_id / vndb_id / metadata_source
    //      c) emit meta-fetch-progress { game_id, phase: "started" }
    //      d) 分支：
    //         ─ 未绑定：
    //            let result = ingest::refresh_for_query(id, &data_dir, &path, &name, exec.as_deref()).await;
    //            // 沿用 refresh_all_metadata:1301-1328 的 UPDATE shape；保留 write_staff_and_tags 调用
    //         ─ 已绑定：
    //            // 选 source/id（复用 backfill_release_year:3557-3575 那段 match 逻辑；
    //            // metadata_source = "bangumi" → bangumi_id；"vndb" → vndb_id；
    //            // 其它（含 manual / None）→ bangumi_id 优先，否则 vndb_id；都没就 continue）
    //            let detail = match source_enum { Bangumi: bangumi::fetch_detail(&sid).await,
    //                                             Vndb:    vndb::fetch_detail(&sid).await,
    //                                             _ => { /* finished + continue */ } };
    //            let persons = match source_enum { Bangumi: bangumi::fetch_persons(&sid).await.unwrap_or_default(),
    //                                              Vndb:    vndb::fetch_persons(&sid).await.unwrap_or_default(),
    //                                              _ => vec![] };
    //            let characters = match source_enum { Bangumi: bangumi::fetch_characters(&sid).await.unwrap_or_default(),
    //                                                 Vndb:    vndb::fetch_characters(&sid).await.unwrap_or_default(),
    //                                                 _ => vec![] };
    //            // 拼装 staff = persons + characters
    //            // 解析 release_year = ingest::parse_release_year(detail.release_date.as_deref())
    //            // 解析 age_rating = detail.is_r18.map(|b| if b { "r18" } else { "all_ages" })
    //            // UPDATE games SET
    //              cover_path = COALESCE(?, cover_path),     // ← detail.cover_url 写到 cover_path？不！
    //                                                          // cover_path 是本地下载路径，已绑定行**不重下封面**，
    //                                                          // 不要触碰 cover_path。改为：cover_url = COALESCE(?, cover_url)
    //                                                          // **唯一**写入；cover_path 留给 cover_cache 流程。
    //              （订正：已绑定路径不动 cover_path，只把 cover_url 用 COALESCE 更新；旧 refresh_all_metadata 写
    //               cover_path 是因为走的是 process_game → cover_cache 路径。这里跳过 cover 下载即可。）
    //
    //              修正后 SQL：
    //              UPDATE games SET
    //                cover_url       = COALESCE(?, cover_url),
    //                summary         = ?,
    //                brand           = COALESCE(?, brand),
    //                age_rating      = COALESCE(?, age_rating),
    //                release_year    = ?,
    //                last_scanned_at = datetime('now')
    //              WHERE id = ?
    //
    //            // 然后构造一个 IngestResult 壳（只填 staff、tags、metadata_source 字符串、其它字段填 row 现值/默认）
    //            // 调 write_staff_and_tags(&pool_for_task, id, &source_str, &result_shell).await
    //            // 若 fetch_detail Err → eprintln + 跳过 UPDATE，仍发 finished 事件
    //      e) emit meta-fetch-progress { game_id, phase: "finished" }
    //      f) emit scan-progress { current_dir: path, completed: i+1, total, Running }
    // 5) 循环结束 emit scan-progress { completed: total, total, Completed }
    Ok(())
}
```

实现要点：
- **不要重下封面**：已绑定行的 cover_path 保留，仅 COALESCE 更新 cover_url（便于未来手动重下；同时不触碰用户已下载的本地图）。这与 backfill_metadata_enrichment 已删函数的"已绑定不动 cover_path"原则一致。
- **未绑定行**沿用旧 refresh_all_metadata 路径——它本身就会 process_game → cover_cache，没问题。
- staff 拼装：`let mut staff = persons; staff.extend(characters);`（参考 ingest.rs:process_game 内的 combine 逻辑——直接首尾相连即可，write_staff_and_tags 用 INSERT OR IGNORE）
- 已绑定行调用 write_staff_and_tags 时需要构造 IngestResult 壳——简化方式：只填 `staff` + `tags` + `metadata_source`（其它字段用 IngestResult::default 风格补 dummy），因为 write_staff_and_tags 只读这三处。如果 IngestResult 无 Default，照搬其它字段从 row 里填（games_path = path.clone()、name = name.clone()、其余 None / 0 / empty）。
- source_str 给 write_staff_and_tags：bangumi 分支传 "bangumi"，vndb 分支传 "vndb"。
- cancel 检查使用 `ctx_for_task.cancel.load(Ordering::Relaxed)`，与 refresh_all_metadata 完全一致。

**B. 删除 commands.rs 中：**
- `pub async fn refresh_all_metadata`（行 1188-1367 整段函数 + 上方文档注释从 "/// Bulk metadata refresh" 起到 `}` 收）
- `pub async fn backfill_release_year`（行 3486-3633 整段函数 + 上方文档注释从 `/// Quick 260513-2nx` 起到 `}` 收；**保留** cancel_backfill 函数）
- 若有 import 因这两个函数才存在的孤儿（例如某些 BackfillState 仅这两处用），由编译器报警决定；预计 BackfillState 还被 cancel_backfill 用 → 保留。MetadataSource 在新函数中复用 → 保留。

**C. 在 src-tauri/src/lib.rs invoke_handler! 中：**
- 删除 `commands::refresh_all_metadata,`（行 204）
- 删除 `commands::backfill_release_year,`（行 253）
- 在 `commands::refresh_metadata,`（行 203）之后插入 `commands::refresh_metadata_smart,`

保留：commands::cancel_backfill（行 258，BackfillState 仍是 Phase 13 资产）、commands::cancel_scan、startScan/start_scan 等。
  </action>
  <verify>
    <automated>cd D:/project/gal-lib/src-tauri && cargo check 2>&1 | tail -30</automated>
  </verify>
  <done>
- cargo check 通过，0 编译错误（warning 容忍但应无新增 unused import）
- `grep -n "pub async fn refresh_all_metadata" src-tauri/src/commands.rs` → 0 hits
- `grep -n "pub async fn backfill_release_year" src-tauri/src/commands.rs` → 0 hits
- `grep -n "pub async fn refresh_metadata_smart" src-tauri/src/commands.rs` → 1 hit
- `grep -nv '^#' src-tauri/src/lib.rs | grep -c "commands::refresh_all_metadata,"` → 0
- `grep -nv '^#' src-tauri/src/lib.rs | grep -c "commands::backfill_release_year,"` → 0
- `grep -nv '^#' src-tauri/src/lib.rs | grep -c "commands::refresh_metadata_smart,"` → 1
  </done>
</task>

<task type="auto">
  <name>Task 2: 前端 — 改 scan.ts/persons.ts/Settings.tsx</name>
  <files>src/lib/scan.ts, src/lib/persons.ts, src/routes/Settings.tsx</files>
  <action>
**A. src/lib/scan.ts**
- 删除 `export async function refreshAllMetadata()`（行 89-98，含上方文档块）
- 新增（建议放在原 refreshAllMetadata 位置）：
```ts
/**
 * Quick 260513-3df — 统一刷新元数据入口。
 * 对未绑定行做模糊匹配（用 games.name 当 query），对已绑定行按 source_id
 * 直拉 fetch_detail/persons/characters（不重做匹配，manual 安全）。
 * 共享 scan-progress + meta-fetch-progress 事件通道；可被 cancelScan 中止。
 */
export async function refreshMetadataSmart(): Promise<void> {
  await invoke("refresh_metadata_smart");
}
```

**B. src/lib/persons.ts**
- 删除 `export async function backfillReleaseYear()`（行 161-173，含上方文档块）
- **保留** `cancelBackfill`（仍可能用于其它 backfill 场景，且本次不删 BackfillState）

**C. src/routes/Settings.tsx**
- 行 54：从 `@/lib/scan` import 中把 `refreshAllMetadata` 去掉，替换为 `refreshMetadataSmart`。最终是：
  ```ts
  import {
    addGame,
    addScanRoot,
    clearAllData,
    listScanRoots,
    refreshMetadataSmart,
    removeScanRoot,
    startScan,
  } from "@/lib/scan";
  ```
- 行 58：整行 `import { backfillReleaseYear } from "@/lib/persons";` **删除**
- 行 259-267：`onRefreshAllMetadata` handler **整段删除**
- 行 269-277：`onBackfillReleaseYear` handler **整段删除**
- 新增 `onRefreshMetadata` handler（放在 onScan 下方）：
  ```ts
  async function onRefreshMetadata() {
    if (scanRoots.length === 0) {
      // 注意：刷新元数据其实不依赖 scan_roots（操作的是 games 表）—— 但 UX 上至少
      // 保证库非空。改为基于 totalRoots 不行（totalRoots = scanRoots.length）。
      // 直接 try invoke；若库为空后端会立即发 Completed 自然结束（参照旧
      // refresh_all_metadata:1229-1240）。所以这里不加前置检查，直接调。
    }
    try {
      await refreshMetadataSmart();
      toast.info("刷新元数据已启动");
      navigate("/");
    } catch (e: unknown) {
      toast.error(`启动失败 — ${String(e)}`);
    }
  }
  ```
  实现版本：去掉占位注释，直接：
  ```ts
  async function onRefreshMetadata() {
    try {
      await refreshMetadataSmart();
      toast.info("刷新元数据已启动");
      navigate("/");
    } catch (e: unknown) {
      toast.error(`启动失败 — ${String(e)}`);
    }
  }
  ```
- 行 492-532（整个 scan-ops `<Section>`）改为：
  ```tsx
  <Section
    id="scan-ops"
    title="扫描操作"
    lede="全量扫描发现并匹配新游戏；刷新元数据对已收录游戏重抓元数据（已绑定的按 ID 直拉、未绑定的走模糊匹配）"
    sectionRefs={sectionRefs}
  >
    <div className="flex flex-wrap gap-2.5">
      <SettingButton primary onClick={() => void onScan("full")}>
        全量扫描
      </SettingButton>
      <SettingButton onClick={() => void onRefreshMetadata()}>
        刷新元数据
      </SettingButton>
    </div>
  </Section>
  ```
  **删除**：「增量扫描」button、「补全发行年份」button、「强制刷新全部元数据」button、AlertDialog（含其内 AlertDialogContent / Header / Description / Footer / Cancel / Action）。
- AlertDialog 仍被「扫描根目录」节的 `<AlertDialog>` (行 405-430，确认移除根目录) 使用 → **保留 import**（行 31-41 的 AlertDialog 一组导入不要全删）。

确认：删除后 Settings.tsx 中：
- `AlertDialog` import 仍保留（scan-roots 仍在用）
- `refreshAllMetadata` / `backfillReleaseYear` / `onRefreshAllMetadata` / `onBackfillReleaseYear` / "增量扫描" / "补全发行年份" / "强制刷新全部元数据" 字符串全部消失
  </action>
  <verify>
    <automated>cd D:/project/gal-lib && pnpm tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
- `pnpm tsc --noEmit` 通过（0 error）
- `grep -nv '^//' src/lib/scan.ts | grep -c "refreshAllMetadata"` → 0
- `grep -nv '^//' src/lib/scan.ts | grep -c "refreshMetadataSmart"` → ≥ 2（function + invoke）
- `grep -nv '^//' src/lib/persons.ts | grep -c "backfillReleaseYear"` → 0
- `grep -nv '^//' src/routes/Settings.tsx | grep -c "refreshAllMetadata"` → 0
- `grep -nv '^//' src/routes/Settings.tsx | grep -c "backfillReleaseYear"` → 0
- `grep -nv '^//' src/routes/Settings.tsx | grep -c "增量扫描"` → 0
- `grep -nv '^//' src/routes/Settings.tsx | grep -c "补全发行年份"` → 0
- `grep -nv '^//' src/routes/Settings.tsx | grep -c "强制刷新全部元数据"` → 0
- `grep -nv '^//' src/routes/Settings.tsx | grep -c "onRefreshMetadata"` → 1
- `grep -nv '^//' src/routes/Settings.tsx | grep -c "refreshMetadataSmart"` → ≥ 1
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 人工 smoke — 启动 app，按下两个按钮，肉眼验证</name>
  <what-built>Settings「扫描操作」节简化为 2 按钮 + 新后端 IPC refresh_metadata_smart 已实现并通过编译。</what-built>
  <how-to-verify>
1. `cd D:/project/gal-lib && pnpm tauri dev` 启动 app
2. 打开 Settings 页 → 滚到「扫描操作」节，确认：
   - 只有 2 个按钮：**全量扫描**（primary 蓝） + **刷新元数据**
   - 没有「增量扫描」「补全发行年份」「强制刷新全部元数据」
   - 没有任何确认 AlertDialog（点「刷新元数据」直接执行，不弹框）
   - lede 文案是新的（包含「全量扫描发现并匹配新游戏；刷新元数据对已收录游戏...」）
3. 点击「全量扫描」→ 跳到主页，ScanProgressBar 出现（验证旧路径未坏）
4. 等扫完或 cancel 之后再回 Settings → 点击「刷新元数据」：
   - toast 显示「刷新元数据已启动」
   - 自动跳主页，ScanProgressBar / BackfillProgressBar 出现进度
   - 单卡片有 meta-fetch 脉冲高亮（与现有 refresh_all_metadata 同样的 UX）
5. 等若干张卡片完成后，**右键挑一张 manual 绑定的游戏**（如果库里没有 manual 绑定，跳到 6）：
   - 打开 Detail 页 → 确认 name / metadata_source = "manual"（如果 UI 显示该字段）未变
   - 如果不显示 metadata_source，回 SQLite 用 dbeaver/cli 查 `SELECT id, name, bangumi_id, vndb_id, metadata_source FROM games WHERE metadata_source = 'manual';` 比对刷新前后这几列**未变**
6. **挑一张已绑定但年份缺失**的游戏（260513-2nx 之前历史绑定）：
   - 确认刷新后 `release_year` 已填上、`summary` 是新内容（直接覆盖）
7. 在刷新中途点 ScanProgressBar 的取消按钮（共享 cancel_scan）→ 进度条应停止、显示 Cancelled
8. cargo build + pnpm build smoke：
   ```
   cd D:/project/gal-lib/src-tauri && cargo check
   cd D:/project/gal-lib && pnpm tsc --noEmit
   ```
   两条都过

**预期结果：**
- Settings 仅剩 2 按钮（截图回复用以确认 UI）
- manual 行的 metadata_source/bangumi_id/vndb_id/name 在刷新后未变（SQL 比对截图）
- 历史无年份行刷新后 release_year 填上
- 取消按钮工作
- 两个编译命令绿
  </how-to-verify>
  <resume-signal>回复 "approved" + 2 按钮截图 + manual 不变的 SQL 比对截图（或描述）；或描述发现的问题。</resume-signal>
</task>

</tasks>

<verification>
- cargo check 通过
- pnpm tsc --noEmit 通过
- grep 检查 7 项 absent + 4 项 contains 全部满足（见 Task 1/2 done block）
- 人工 smoke 通过（Task 3）
</verification>

<success_criteria>
- Settings「扫描操作」节仅剩 2 按钮：全量扫描 + 刷新元数据
- refresh_all_metadata、backfill_release_year 两个后端 IPC 与其前端 wrapper、handler、按钮、AlertDialog 全部彻底删除
- 新 IPC refresh_metadata_smart 对未绑定与已绑定行都能正确处理；manual 行的 bangumi_id/vndb_id/metadata_source/name/name_cn 不被修改
- 已绑定行的 summary/release_year 被直接覆盖（用户主动刷新预期），brand/age_rating/cover_url/cover_path 走 COALESCE 不丢 manual 修改
- 共享 ScanState 使 cancel_scan 可中止；BackfillProgressBar.tsx 0 改动
- cargo check + pnpm tsc --noEmit 全绿
</success_criteria>

<output>
After completion, create `.planning/quick/260513-3df-two-button-metadata-refresh/260513-3df-SUMMARY.md`
</output>
