---
phase: quick-260519-l9n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/launch/session.rs
  - src-tauri/src/commands.rs
  - src-tauri/src/scan/removed_marker.rs
  - src-tauri/src/scan/mod.rs
  - src-tauri/src/scan/types.rs
  - src-tauri/src/lib.rs
  - src/lib/games.ts
  - src/lib/scanReview.ts
  - src/components/library/RemovedDirs.tsx
  - src/routes/Scan.tsx
autonomous: false
requirements: [L9N-01, L9N-02]

must_haves:
  truths:
    - "有累计游玩时长的游戏，若状态仍为 unplayed，会自动变为 playing"
    - "已通关(cleared)/已弃坑(dropped)的游戏即使有时长也不会被降级或改写"
    - "从库中删除游戏后，该游戏磁盘目录里写入了一个隐藏标记文件"
    - "下次增量/全量扫描会跳过带标记的目录，不会把它加回库"
    - "Scan 复核界面显示一个『已删除条目』区域，列出所有带标记的目录"
    - "用户在该区域点击『重新添加』后，标记文件被移除，目录作为新条目导入库"
  artifacts:
    - path: "src-tauri/src/scan/removed_marker.rs"
      provides: "removed-marker 文件名常量 + 写/删/检测/枚举辅助函数"
      min_lines: 40
    - path: "src/components/library/RemovedDirs.tsx"
      provides: "Scan 页『已删除条目』区域组件"
      min_lines: 50
  key_links:
    - from: "src-tauri/src/launch/session.rs end_session/cancel_session"
      to: "games.status"
      via: "时长滚入后条件 UPDATE status"
      pattern: "status='playing'"
    - from: "src-tauri/src/commands.rs delete_game"
      to: "removed_marker::write_marker"
      via: "删除 DB 行后写磁盘标记"
      pattern: "write_marker"
    - from: "src-tauri/src/scan/mod.rs run_scan"
      to: "removed_marker::has_marker"
      via: "每个目录迭代时检测标记并跳过 + 收集"
      pattern: "has_marker"
    - from: "src/components/library/RemovedDirs.tsx"
      to: "restore_removed_dir IPC"
      via: "invoke"
      pattern: "restore_removed_dir"
---

<objective>
两个独立功能改进，同属"库条目状态/生命周期"领域，合并为一个 quick 计划：

1. **自动『游玩中』状态**：有累计游玩时长的条目自动升级为 `playing` 状态，不覆盖用户已手动设置的更高级状态（`cleared`/`dropped`）。
2. **删除标记文件 + 复核界面恢复**：删除条目时在磁盘目录写隐藏标记文件，扫描跳过带标记目录，Scan 页新增『已删除条目』区域支持重新添加。

Purpose: 让"玩过的游戏"在库里有正确状态显示；让"用户主动删除的游戏"不会在每次扫描后阴魂不散地回来，同时保留反悔通道。
Output: 后端 status 自动升级逻辑 + removed-marker 模块 + 3 个新 IPC 命令；前端『已删除条目』区域 UI。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- 从代码库实地核实的契约。执行者直接据此实现，无需再探索。 -->

games 表 status 字段（migrations/0001_init.sql:18）：
  status TEXT NOT NULL DEFAULT 'unplayed'
  CHECK(status IN ('unplayed','playing','cleared','dropped'))
  - 'unplayed' = 未玩（默认）
  - 'playing'  = 游玩中  ← 自动升级目标
  - 'cleared'  = 已通关  ← 用户级状态，不可被自动改写
  - 'dropped'  = 已弃坑  ← 用户级状态，不可被自动改写

会话结束把时长滚入 games（src-tauri/src/launch/session.rs）：
  end_session(pool, session_id, exit_code)   → status='completed' + games.total_playtime_sec += dur
  cancel_session(pool, session_id)           → status='cancelled' + games.total_playtime_sec += dur
  两者都用子查询 `WHERE id = (SELECT game_id FROM sessions WHERE id=?)` 定位 games 行。
  mark_failed 不滚时长（不需要升级状态）。

删除条目命令（src-tauri/src/commands.rs:1083）：
  #[tauri::command]
  pub async fn delete_game(game_id: i64, state: State<'_, AppPaths>) -> Result<(), String>
  - 先逐表删 8 张 game_id 子表，再删 games 行
  - 当前明确"on-disk game folder/files are deliberately NOT touched"

扫描跳过逻辑（src-tauri/src/scan/mod.rs run_scan）：
  pub async fn run_scan<F>(roots, existing_paths: HashSet<PathBuf>, incremental: bool,
                           ctx: Arc<ScanContext>, on_progress: F) -> Result<Vec<DiscoveredGame>, ScanError>
  - Pass 2 中每个 game dir 迭代：先查 ctx.skip 用户跳过集，再查 incremental && existing_paths
  - 跳过时仍 emit ScanProgress(Running, completed: i+1) 并 continue
  - start_scan（commands.rs:571-598）始终以 incremental=true 调用，existing_paths = 已绑定 games 行 ∪ scan_skip_dirs 表

单条目导入（可复用做"重新添加"）：
  async fn ingest_one_dir(pool: &SqlitePool, data_dir: &Path, dg: &scan::DiscoveredGame) -> Result<i64, String>
  add_game 命令构造 DiscoveredGame 的模式（commands.rs:915-930）：
    raw_name = dir.file_name() → string
    clean_name = crate::title_clean::clean_title(&raw_name)
    executable = scan::walker::pick_best_exe(&dir)
    DiscoveredGame { path, raw_name, clean_name, executable }

前端 IPC 封装：
  src/lib/games.ts:196  deleteGame(gameId) → invoke("delete_game", { gameId })
  src/lib/scanReview.ts  已有 listScanReviewQueue / acceptReviewCandidate 等模式（snake_case 不转换，gameId 驼峰）

Scan 页（src/routes/Scan.tsx）：
  布局 = ScanProgressBar / PageHeader / KpiStrip(grid-12) / TwoColumnFeed(ScanFeed 5 + ReviewQueue 7)
  ReviewQueue 组件在 src/components/library/ReviewQueue.tsx，是右栏。
  新区域应作为独立 section 加在 TwoColumnFeed 下方（mt-6），不挤占现有两栏布局。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 自动『游玩中』状态升级 — session 结束 + 一次性历史补齐</name>
  <files>src-tauri/src/launch/session.rs, src-tauri/src/commands.rs, src-tauri/src/lib.rs</files>
  <behavior>
    - end_session 滚入时长后：若该 games 行 status='unplayed' 则改为 'playing'；status 为 'playing'/'cleared'/'dropped' 时不动。
    - cancel_session 同样处理（取消的会话时长也是真实游玩）。
    - 升级用单条带守卫的 UPDATE：`UPDATE games SET status='playing' WHERE id=(...) AND status='unplayed' AND total_playtime_sec>0`，CHECK 约束天然保证取值合法，守卫保证不覆盖用户级状态。
    - 一次性历史补齐命令 backfill_playing_status：对所有 `status='unplayed' AND total_playtime_sec>0` 的行批量 UPDATE 为 'playing'，返回受影响行数。
    - 历史补齐对 status='cleared'/'dropped' 但有时长的行不改写。
  </behavior>
  <action>
在 src-tauri/src/launch/session.rs 的 end_session 中，在已有"roll up into games row"的 UPDATE 之后，追加一条状态升级 UPDATE：
  ```rust
  // L9N-01 — 有真实游玩时长且状态仍为默认 unplayed 时自动升级为 playing。
  // 守卫条件 status='unplayed' 保证不覆盖用户手动设置的 cleared/dropped。
  sqlx::query(
      "UPDATE games SET status='playing', updated_at=datetime('now') \
       WHERE id = (SELECT game_id FROM sessions WHERE id=?) \
         AND status='unplayed' AND total_playtime_sec > 0",
  )
  .bind(session_id)
  .execute(pool)
  .await?;
  ```
在 cancel_session 中追加完全相同的一段（同样的子查询定位 + 守卫）。
mark_failed 不改（它不滚时长）。

在 src-tauri/src/commands.rs 新增一次性历史补齐命令（放在 update_game_status 附近）：
  ```rust
  /// L9N-01 — 一次性把"有累计游玩时长但状态仍为 unplayed"的历史条目升级为 playing。
  /// status 为 cleared/dropped 的行即使有时长也不改写（用户手动设置优先）。
  /// 返回升级的行数。幂等：再次调用不再有未升级行时返回 0。
  #[tauri::command]
  pub async fn backfill_playing_status(state: State<'_, AppPaths>) -> Result<i64, String> {
      let pool = state.pool().await.map_err(err_str)?;
      let result = sqlx::query(
          "UPDATE games SET status='playing', updated_at=datetime('now') \
           WHERE status='unplayed' AND total_playtime_sec > 0",
      )
      .execute(&*pool)
      .await
      .map_err(err_str)?;
      Ok(result.rows_affected() as i64)
  }
  ```
在 src-tauri/src/lib.rs 的 invoke_handler 列表注册 `commands::backfill_playing_status`。

在 session.rs 的 #[cfg(test)] mod tests 的 _signatures_compile 之外，确认现有编译型测试不受影响（仅签名引用，无需新测试文件——session.rs 现有测试是 compile-only smoke，照其约定即可）。若要加测试，在 session.rs tests 内加一个使用内存 SqlitePool 的 #[tokio::test]，验证：插入一个 unplayed + total_playtime_sec=0 的 game → start/end session 后 status='playing'；插入一个 cleared 的 game → end session 后 status 仍='cleared'。若内存 pool 搭建成本过高（需跑 migrations），改为在 commands 层不另写测试，依赖 cargo build + 下方 verify。
  </action>
  <verify>
    <automated>cd src-tauri && cargo build 2>&1 | Select-String -Pattern "error" -NotMatch | Out-Null; cargo test --lib session 2>&1 | Select-String "test result"</automated>
  </verify>
  <done>cargo build 通过；end_session/cancel_session 各含一条带 `status='unplayed'` 守卫的升级 UPDATE；backfill_playing_status 命令已注册到 lib.rs；grep `status='playing'` src-tauri/src/launch/session.rs 命中 2 次。</done>
</task>

<task type="auto">
  <name>Task 2: removed-marker 模块 + delete_game 写标记 + 扫描跳过并收集</name>
  <files>src-tauri/src/scan/removed_marker.rs, src-tauri/src/scan/mod.rs, src-tauri/src/scan/types.rs, src-tauri/src/commands.rs, src-tauri/src/lib.rs</files>
  <action>
新建 src-tauri/src/scan/removed_marker.rs —— removed-marker 文件的读写检测模块：
  ```rust
  //! L9N-02 — "已从库中移除"磁盘标记文件。
  //!
  //! delete_game 在游戏目录内写一个隐藏标记文件 `.gal-lib-removed`，
  //! 扫描时检测到该文件即跳过该目录（不再自动加回库）。
  //! restore_removed_dir 删除标记并把目录作为新条目重新导入。
  use std::path::Path;

  /// 标记文件名 —— 点号前缀避免与游戏文件冲突，Windows 上额外设隐藏属性。
  pub const MARKER_FILENAME: &str = ".gal-lib-removed";

  /// 在 `dir` 内写入标记文件并（Windows）设隐藏属性。已存在则视为成功。
  pub fn write_marker(dir: &Path) -> std::io::Result<()> {
      let marker = dir.join(MARKER_FILENAME);
      // 内容写一个 RFC3339 时间戳，便于将来排查；内容本身不参与逻辑。
      std::fs::write(&marker, chrono::Utc::now().to_rfc3339())?;
      set_hidden(&marker);
      Ok(())
  }

  /// 删除标记文件。文件不存在视为成功（幂等）。
  pub fn remove_marker(dir: &Path) -> std::io::Result<()> {
      let marker = dir.join(MARKER_FILENAME);
      match std::fs::remove_file(&marker) {
          Ok(()) => Ok(()),
          Err(e) if e.kind() == std::io::ErrorKind::NotFound => Ok(()),
          Err(e) => Err(e),
      }
  }

  /// `dir` 是否带 removed 标记。用 `Path::join + exists` —— 单次 stat，扫描热路径可接受。
  pub fn has_marker(dir: &Path) -> bool {
      dir.join(MARKER_FILENAME).exists()
  }

  #[cfg(windows)]
  fn set_hidden(path: &Path) {
      // best-effort：失败不影响功能（隐藏只是观感）。
      use std::os::windows::ffi::OsStrExt;
      let wide: Vec<u16> = path.as_os_str().encode_wide().chain(std::iter::once(0)).collect();
      const FILE_ATTRIBUTE_HIDDEN: u32 = 0x2;
      unsafe {
          // 已链接的 winapi/windows crate：若项目已有依赖优先复用其 SetFileAttributesW；
          // 否则用 std 无法设隐藏 —— 此时退化为不设隐藏（标记仍生效，仅可见）。
          // 执行者：检查 src-tauri/Cargo.toml 是否已有 windows / winapi crate，
          // 有则调用对应 SetFileAttributesW；没有则删掉本 unsafe 块、保留点前缀文件名即可。
          let _ = (&wide, FILE_ATTRIBUTE_HIDDEN);
      }
  }
  #[cfg(not(windows))]
  fn set_hidden(_path: &Path) {}
  ```
  注意：执行者先 grep `src-tauri/Cargo.toml` 看是否已有 `windows` 或 `winapi` 依赖。
  - 若有：用真实的 `SetFileAttributesW` 调用替换上面 unsafe 块的占位。
  - 若无：直接删掉 `set_hidden` 的 Windows 分支实现体，保留空函数 —— `.gal-lib-removed` 点前缀文件名本身已满足"避免与游戏文件冲突"，隐藏属性是 nice-to-have，不为它新增 crate 依赖。
  在 src-tauri/src/scan/mod.rs 顶部加 `pub mod removed_marker;`。

在 src-tauri/src/scan/mod.rs 的 run_scan Pass 2 中，在"Incremental mode 跳过 existing_paths"判断**之后**、`pick_best_exe` 之前，新增 removed-marker 检测：
  ```rust
  // L9N-02 — 带 removed 标记的目录：用户主动删除过，跳过不加回，并记入收集列表。
  if removed_marker::has_marker(&dir) {
      removed_dirs.push(dir.clone());
      on_progress(ScanProgress { current_dir: dir.to_string_lossy().into_owned(),
          completed: i + 1, total, status: ScanStatus::Running, phase: ScanPhase::Discovering });
      continue;
  }
  ```
  run_scan 需要把收集到的 removed 目录返回给调用方。改 run_scan 返回类型：
  当前 `Result<Vec<DiscoveredGame>, ScanError>` → `Result<ScanOutcome, ScanError>`，
  在 src-tauri/src/scan/types.rs 新增：
  ```rust
  /// run_scan 的完整产出 —— 正常发现的游戏 + 被 removed 标记跳过的目录。
  #[derive(Debug)]
  pub struct ScanOutcome {
      pub discovered: Vec<DiscoveredGame>,
      pub removed_dirs: Vec<std::path::PathBuf>,
  }
  ```
  在 scan/mod.rs 的 `pub use types::{...}` 加上 `ScanOutcome`。run_scan 内声明 `let mut removed_dirs: Vec<PathBuf> = Vec::new();`，结尾 `Ok(ScanOutcome { discovered, removed_dirs })`。
  更新 scan/mod.rs 内 4 个现有 #[tokio::test]：原来 `.expect(...)` 拿到的是 Vec，现在改为 `.expect(...).discovered`（如 `result.len()` → `result.discovered.len()`）。

在 src-tauri/src/commands.rs：
  - delete_game（:1083）末尾、`Ok(())` 之前，新增写标记。从 games 行删除前需先拿到 path —— 在删子表前先查 path：
    ```rust
    // L9N-02 — 删除前取目录路径，删除后写 removed 标记（best-effort，标记失败不阻断删除）。
    let game_path: Option<String> = sqlx::query_scalar("SELECT path FROM games WHERE id = ?")
        .bind(game_id).fetch_optional(&*pool).await.map_err(err_str)?;
    ```
    放在函数开头 pool 拿到之后。然后在末尾、确认 rows_affected>0 之后：
    ```rust
    if let Some(p) = game_path {
        if let Err(e) = scan::removed_marker::write_marker(std::path::Path::new(&p)) {
            eprintln!("[delete_game] write removed-marker failed for {}: {}", p, e);
        }
    }
    ```
  - start_scan 的 spawn 任务里，`scan::run_scan(...)` 现在返回 `ScanOutcome`。把 `let discovered = match scan_res { Ok(d) => d, ... }` 改为 `Ok(o) => o.discovered`，并在拿到 outcome 后忽略 `removed_dirs`（start_scan 不直接用 —— 复核界面通过下方 Task 3 的独立 IPC 单独枚举）。即解构出 `discovered` 字段使用即可。
  - 新增枚举命令 list_removed_dirs：遍历所有 scan_roots，对每个根用 `scan::walker::collect_game_dirs` 收集 game 目录，过滤出 `removed_marker::has_marker(&dir)` 的，返回路径列表。**为了不与扫描进度耦合且实现简单**：
    ```rust
    /// L9N-02 — 枚举所有 scan_roots 下带 removed 标记的目录。供 Scan 页『已删除条目』区域。
    #[tauri::command]
    pub async fn list_removed_dirs(state: State<'_, AppPaths>) -> Result<Vec<String>, String> {
        let pool = state.pool().await.map_err(err_str)?;
        let roots: Vec<(PathBuf, u8)> = { /* 复用 start_scan 读 scan_roots 的同款查询 */ };
        let cancel = std::sync::Arc::new(std::sync::atomic::AtomicBool::new(false));
        let dirs = scan::walker::collect_game_dirs(&roots, &cancel).map_err(err_str)?;
        Ok(dirs.into_iter()
            .filter(|d| scan::removed_marker::has_marker(d))
            .map(|d| d.to_string_lossy().into_owned())
            .collect())
    }
    ```
    （执行者核实 `collect_game_dirs` 签名 —— 见 scan/mod.rs:88 `walker::collect_game_dirs(&roots, &ctx.cancel)`，参数是 `&Vec<(PathBuf,u8)>` 与 `&Arc<AtomicBool>`。）
  - 新增恢复命令 restore_removed_dir：删标记 + 复用 ingest_one_dir 导入。
    ```rust
    /// L9N-02 — 把一个被标记为已删除的目录重新加回库：删磁盘标记 + 作为新条目导入。
    #[tauri::command]
    pub async fn restore_removed_dir(path: String, state: State<'_, AppPaths>) -> Result<i64, String> {
        let dir = PathBuf::from(&path);
        if !dir.is_dir() { return Err(format!("目录不存在: {}", path)); }
        let pool = state.pool().await.map_err(err_str)?;
        let data_dir = state.data_dir.clone();
        // 先删标记，否则导入后下次扫描仍会被跳过。
        scan::removed_marker::remove_marker(&dir).map_err(err_str)?;
        // 复用 add_game 的 DiscoveredGame 构造模式（commands.rs:915-930）。
        let raw_name = dir.file_name().and_then(|s| s.to_str()).unwrap_or("").to_string();
        let clean_name = crate::title_clean::clean_title(&raw_name);
        let executable = scan::walker::pick_best_exe(&dir);
        let dg = scan::DiscoveredGame { path: dir, raw_name, clean_name, executable };
        ingest_one_dir(&*pool, &data_dir, &dg).await
    }
    ```
  - 在 src-tauri/src/lib.rs 注册 `commands::list_removed_dirs` 和 `commands::restore_removed_dir`。
  </action>
  <verify>
    <automated>cd src-tauri && cargo build 2>&1 | Select-String "error\[|error:" | Out-Null; cargo test --lib scan 2>&1 | Select-String "test result"</automated>
  </verify>
  <done>cargo build 通过；scan 模块全部单元测试通过（4 个 run_scan 测试已适配 ScanOutcome）；delete_game 末尾写 .gal-lib-removed；run_scan 跳过带标记目录；list_removed_dirs / restore_removed_dir 已注册；removed_marker.rs 含 write/remove/has 三函数。</done>
</task>

<task type="auto">
  <name>Task 3: 前端『已删除条目』区域 + IPC 封装 + Scan 页接入</name>
  <files>src/lib/games.ts, src/lib/scanReview.ts, src/components/library/RemovedDirs.tsx, src/routes/Scan.tsx</files>
  <action>
在 src/lib/scanReview.ts（或 games.ts —— 选 scanReview.ts，因为是 Scan 页用）追加两个 IPC 封装：
  ```ts
  /** L9N-02 — 枚举所有 scan_roots 下带 .gal-lib-removed 标记的目录绝对路径。 */
  export async function listRemovedDirs(): Promise<string[]> {
    return invoke<string[]>("list_removed_dirs");
  }
  /** L9N-02 — 把一个被标记删除的目录重新加回库：删标记 + 导入。返回新 games.id。 */
  export async function restoreRemovedDir(path: string): Promise<number> {
    return invoke<number>("restore_removed_dir", { path });
  }
  ```
  （scanReview.ts 顶部已 `import { invoke } from "@tauri-apps/api/core"`，直接复用。）
  注意：`delete_game` 现在会写磁盘标记 —— src/lib/games.ts 的 deleteGame 注释里"row 删除后磁盘不动"的描述若有，更新为"删 DB 行 + 写 .gal-lib-removed 标记，避免下次扫描加回"。

新建 src/components/library/RemovedDirs.tsx —— Scan 页『已删除条目』区域组件：
  - 顶部标题栏样式参照 ReviewQueue.tsx 的 header（`font-mono text-[10.5px] uppercase tracking-[0.14em] text-ink-3`，"已删除条目" + 右侧计数）。
  - mount 时调 listRemovedDirs() 填列表；列表为空时显示空态提示文案（如"没有被标记删除的目录 —— 删除游戏会在此可恢复"），参照 ReviewQueue 空态样式。
  - 每行：目录路径（`font-mono` 截断显示）+ 一个"重新添加"按钮。
  - 点"重新添加"：调 restoreRemovedDir(path)，乐观地从本地列表移除该行；成功 toast.success(`已重新加入「${basename}」`)，失败 toast.error 并 refetch 回滚。成功后调用 props.onRestored?.() 让父组件刷新 KPI / 触发 games-changed。
  - 边框/圆角用 `border border-line bg-bg-1` + `style={{ borderRadius: "var(--r-md)" }}`，与 ReviewQueue 容器一致。
  - props 接口：`{ onRestored?: () => void }`。
  - 提供具名 export `RemovedDirs`。

在 src/routes/Scan.tsx 接入：
  - import `RemovedDirs`。
  - 在 TwoColumnFeed（`<div className="mt-6 grid ... lg:grid-cols-12">`）**之后**新增一个 section：`<div className="mt-6"><RemovedDirs onRestored={() => void refreshKpis()} /></div>`。
  - 不改动现有 KPI strip 与两栏布局。
  </action>
  <verify>
    <automated>npm run build 2>&1 | Select-String -Pattern "error|Error" | Select-String -NotMatch "0 error"</automated>
  </verify>
  <done>npm run build（含 tsc）通过无类型错误；RemovedDirs.tsx 导出 RemovedDirs 组件；Scan.tsx 在两栏 feed 下方渲染 RemovedDirs；scanReview.ts 含 listRemovedDirs + restoreRemovedDir；前端无 lint 报错。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端 → Tauri 命令 | restore_removed_dir 接收前端传入的 path 字符串 |
| Tauri 命令 → 文件系统 | write/remove marker、collect_game_dirs 遍历用户磁盘 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-l9n-01 | Tampering | restore_removed_dir 的 path 参数 | mitigate | `dir.is_dir()` 校验路径真实存在再操作；ingest_one_dir 走既有 add_game 同款路径，不引入新攻击面 |
| T-l9n-02 | Denial of Service | list_removed_dirs 遍历所有 scan_roots | accept | 复用既有 collect_game_dirs（扫描本就遍历同样目录树），开销与一次增量扫描同级，单机本地应用可接受 |
| T-l9n-03 | Information Disclosure | .gal-lib-removed 标记文件内容 | accept | 仅写 RFC3339 时间戳，无敏感信息；文件在用户自己的游戏目录内 |
| T-l9n-04 | Tampering | 自动 status 升级误改用户级状态 | mitigate | UPDATE 带 `status='unplayed'` 守卫，CHECK 约束兜底；cleared/dropped 永不被自动改写 |
</threat_model>

<verification>
- `cd src-tauri && cargo build` 通过，`cargo test --lib` 全绿（含适配后的 scan run_scan 测试）。
- `npm run build` 通过，无 TypeScript / lint 错误。
- 代码核查：end_session + cancel_session 各含一条带 `status='unplayed'` 守卫的升级 UPDATE；mark_failed 不含。
- 代码核查：delete_game 末尾写 `.gal-lib-removed`；run_scan 检测 has_marker 并跳过 + 收集；3 个新 IPC 全部注册到 lib.rs。
</verification>

<success_criteria>
- 有累计游玩时长（total_playtime_sec > 0）且状态为 unplayed 的条目，会话结束后或经 backfill 后变为 playing。
- 状态为 cleared / dropped 的条目即使有时长也不被改写。
- 删除条目在其磁盘目录留下隐藏标记文件 .gal-lib-removed。
- 带标记的目录在下次扫描时被跳过，不会重新加回库。
- Scan 复核页在两栏 feed 下方显示『已删除条目』区域，列出所有带标记目录。
- 用户点『重新添加』后，标记文件被删、目录作为新条目导入库、列表行消失。
</success_criteria>

<output>
完成后创建 `.planning/quick/260519-l9n-playing-status-delete-marker/260519-l9n-SUMMARY.md`
</output>
