---
quick_id: 20260510
slug: ui-fixes-detail-cards-filter-brands
date: 2026-05-10
status: complete
---

# Quick Task 20260510 — Summary

修复了 4 个 UI 问题。所有 wire 协议错配、布局过长、菜单缺项均已闭合。

## 实际改动

### 后端（`src-tauri/src/commands.rs`）

- `GameStaffRow.id` 加 `#[serde(rename = "person_id")]`
  - 修复：详情页人物 chip 点击跳到 `/persons/undefined`、`Persons.tsx` 显示「无效的人物 id」
  - `Persons.tsx:119/147` 的 identity find 也同时修好
- `PersonOption` 加 `pub count: i64` + `persons_for_role` 装载 `cnt`
- 新增 `pub struct BrandOption { name, count }`，`FilterOptions.brands` 类型从 `Vec<String>` 改为 `Vec<BrandOption>`
  - 修复：FilterPanel 品牌区显示 `undefined · undefined`

### 前端

- `src/components/library/FilterPanel.tsx:182-185` PopoverContent 背景从 `bg-bg-1/95 ... backdrop-blur-md` 改为纯色 `bg-bg-1`
  - 修复：筛选弹层透明问题
- `src/components/layout/Sidebar.tsx` 品牌 section 包了一层 `max-h-[300px] overflow-y-auto`（约 10 行高度）
  - 修复：品牌列表过长把发行年份挤出视口
- `src/routes/Detail.tsx` "更多" dropdown 加：
  - `DropdownMenuSeparator`
  - 「重新匹配元数据」→ 打开 MetadataPicker modal
  - 「重新抓取封面」→ `refreshMetadata(gameId)` + 串行刷新 game/staff/officialTags
  - 末尾渲染 `<MetadataPicker>`，受控于新增的 `pickerOpen` state
  - `onClosePicker` 在 modal 关闭后串行刷 game/staff/tags，确保新元数据落地

## 不在范围内（明确拒绝）

- "音乐 · 4" 把 "あーるず"/"神田佐知" 显示为音乐人 — 这是 Bangumi/VNDB 角色分类落库的事，不在 UI 范围。本任务只解决了 chip 点击跳错页的问题。
- 启动 / 强制结束 / 收藏 / 通关状态 — Detail 页已有等价 UI（LaunchButton / Heart / 常用操作 Select），不在"更多"里重复添加。

## 验证

- `cargo check` ✅（5 个无关 warning，全部已存在）
- `npx tsc --noEmit` ✅
- 真机 UI 校验：autonomous 模式下不能跑窗口程序，留给用户启动后人眼校验。must_haves 已通过编译层面验证。
