---
phase: quick-260519-p90
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src-tauri/tauri.conf.json
  - src-tauri/Cargo.toml
  - src-tauri/Cargo.lock
autonomous: true
requirements: [RELEASE-v0.2.3]
must_haves:
  truths:
    - "四处版本字段全部为 0.2.3"
    - "Cargo.lock 仅 gal-lib 自身条目的 version 变更，其余依赖条目不动"
    - "版本号 bump 组成单一原子提交"
  artifacts:
    - path: "package.json"
      provides: "前端包版本号 0.2.3"
      contains: '"version": "0.2.3"'
    - path: "src-tauri/tauri.conf.json"
      provides: "Tauri 应用版本号 0.2.3"
      contains: '"version": "0.2.3"'
    - path: "src-tauri/Cargo.toml"
      provides: "Rust crate 版本号 0.2.3"
      contains: 'version = "0.2.3"'
    - path: "src-tauri/Cargo.lock"
      provides: "gal-lib 锁文件条目版本号 0.2.3"
      contains: 'name = "gal-lib"'
  key_links:
    - from: "src-tauri/Cargo.toml"
      to: "src-tauri/Cargo.lock"
      via: "gal-lib [[package]] 条目 version 同步"
      pattern: 'name = "gal-lib"'
---

<objective>
将 gal-lib 版本号从 0.2.2 升级到 0.2.3，为发布 v0.2.3 小版本做准备。

Purpose: 四处版本字段必须统一，否则 Tauri 打包产物、前端 about 信息、Rust crate 元数据三方不一致，发布产物会出现版本号矛盾。
Output: package.json / tauri.conf.json / Cargo.toml / Cargo.lock 四处版本字段统一为 0.2.3，组成单一原子提交。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md

# 已确认的当前状态（无需重新读取，直接 Edit 即可）
# - package.json 第 4 行:           "version": "0.2.2",
# - src-tauri/tauri.conf.json 第 4 行: "version": "0.2.2",
# - src-tauri/Cargo.toml 第 3 行:     version = "0.2.2"
# - src-tauri/Cargo.lock 第 1573 行:  version = "0.2.2"（紧随第 1572 行 name = "gal-lib"）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 升级四处版本字段 0.2.2 → 0.2.3</name>
  <files>package.json, src-tauri/tauri.conf.json, src-tauri/Cargo.toml, src-tauri/Cargo.lock</files>
  <action>
将四处版本字段从 0.2.2 升级到 0.2.3：

1. `package.json` 第 4 行 — 将 `"version": "0.2.2",` 改为 `"version": "0.2.3",`
2. `src-tauri/tauri.conf.json` 第 4 行 — 将 `"version": "0.2.2",` 改为 `"version": "0.2.3",`
3. `src-tauri/Cargo.toml` 第 3 行 — 将 `version = "0.2.2"` 改为 `version = "0.2.3"`
4. `src-tauri/Cargo.lock` — 定位到 `name = "gal-lib"`（第 1572 行）紧随其下的 `version = "0.2.2"`（第 1573 行），改为 `version = "0.2.3"`。

关键约束（与上一次 v0.2.2 发布计划 260517-sm9-PLAN.md 保持一致）：
- Cargo.lock **只改 gal-lib 自身这一个 `[[package]]` 条目的 version**。Cargo.lock 中其余所有依赖条目的 `version` 一律不动。直接 Edit 第 1573 行即可，无需运行 `cargo update`（仅 workspace 自身包版本变化，不影响依赖解析图，也无需联网）。
- 注意时序：编辑 `Cargo.toml` 后，本机后台可能存在的 cargo 进程（rust-analyzer / cargo build）可能已自动把 `Cargo.lock` 的 gal-lib 条目同步到 0.2.3。编辑 Cargo.lock 前先用 Grep 复查 `name = "gal-lib"` 后两行：
  - 若已是 `version = "0.2.3"` → 已被自动同步，跳过该处编辑，无需重复。
  - 若仍是 `version = "0.2.2"` → 按上述第 4 步手动 Edit。
- 不要做任何其他改动：不改 changelog、不改 description、不动依赖。

不要执行 `git tag` / `git push` / `cargo build` / `pnpm build` 或任何发布动作 —— 打 tag v0.2.3 与推送由 orchestrator 在本计划执行后统一处理。
  </action>
  <verify>
    <automated>grep -rn "0.2.3" package.json src-tauri/tauri.conf.json src-tauri/Cargo.toml | grep -c version</automated>
  </verify>
  <done>package.json、tauri.conf.json、Cargo.toml 三处 version 字段均为 0.2.3；Cargo.lock 中 `name = "gal-lib"` 条目的 version 为 0.2.3，且 Cargo.lock 内无其他依赖条目 version 被改动（git diff 仅含 gal-lib 一行 version 变更）。</done>
</task>

</tasks>

<verification>
- `grep -n version package.json` → `"version": "0.2.3"`
- `grep -n version src-tauri/tauri.conf.json` → `"version": "0.2.3"`
- `grep -n version src-tauri/Cargo.toml`（第 3 行）→ `version = "0.2.3"`
- Cargo.lock 中 `name = "gal-lib"` 后一行 → `version = "0.2.3"`
- `git diff --stat` → 仅 4 个文件改动（若 cargo 已自动同步 Cargo.lock 也算 4 个）
- `git diff src-tauri/Cargo.lock` → 仅 1 行 version 变更，无依赖条目改动
</verification>

<success_criteria>
- 四处版本字段全部为 0.2.3，无遗漏
- Cargo.lock 仅 gal-lib 自身条目变更
- 改动组成单一原子提交，提交信息形如 `chore: bump version to 0.2.3`
- 未执行任何 git tag / push / build 动作
</success_criteria>

<output>
After completion, create `.planning/quick/260519-p90-v0-2-3/260519-p90-SUMMARY.md`
</output>
