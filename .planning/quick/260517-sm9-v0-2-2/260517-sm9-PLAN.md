---
phase: quick-260517-sm9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src-tauri/tauri.conf.json
  - src-tauri/Cargo.toml
  - src-tauri/Cargo.lock
autonomous: true
requirements: [sm9-RELEASE-022]
must_haves:
  truths:
    - "三处面向用户/构建系统的版本字段统一显示 0.2.2"
    - "Cargo.lock 中 gal-lib 自身包条目与 Cargo.toml 版本一致 (0.2.2)"
    - "构建产物 (Tauri exe / npm 包) 元数据反映 0.2.2"
  artifacts:
    - path: "package.json"
      provides: "前端包版本字段"
      contains: "\"version\": \"0.2.2\""
    - path: "src-tauri/tauri.conf.json"
      provides: "Tauri 应用版本字段"
      contains: "\"version\": \"0.2.2\""
    - path: "src-tauri/Cargo.toml"
      provides: "Rust crate 版本字段"
      contains: "version = \"0.2.2\""
    - path: "src-tauri/Cargo.lock"
      provides: "gal-lib 自身锁定条目版本"
      contains: "version = \"0.2.2\""
  key_links:
    - from: "src-tauri/Cargo.toml"
      to: "src-tauri/Cargo.lock"
      via: "workspace 包版本必须与其 lock 条目一致"
      pattern: "name = \"gal-lib\"[\\s\\S]*?version = \"0.2.2\""
---

<objective>
将 gal-lib 版本号从 0.2.1 升级到 0.2.2，为发布 v0.2.2 小版本做准备。

Purpose: 三处版本字段 (package.json / tauri.conf.json / Cargo.toml) 与 Cargo.lock 自身条目必须保持一致，否则构建产物元数据不一致、release 资产命名错乱。
Output: 4 个文件各一处版本号变更，组成一个原子提交。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
# 范围严格限定为版本号 bump + Cargo.lock 同步。
# 不要执行 git tag / git push —— 打 tag 与推送由 orchestrator 在本计划执行后统一处理。
# 自 v0.2.1 起的用户可见改动（仅作发布说明背景，非本计划任务）：
#   删除条目功能、详情页启动配置简化（日区 LE / 直接启动）、列表滚动位置恢复、平滑滚动修复。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 将四处版本号从 0.2.1 升到 0.2.2</name>
  <files>package.json, src-tauri/tauri.conf.json, src-tauri/Cargo.toml, src-tauri/Cargo.lock</files>
  <action>
对以下 4 个文件各做一处精确替换，当前值均为 `0.2.1`，目标值 `0.2.2`：

1. `package.json` 第 4 行：`  "version": "0.2.1",` → `  "version": "0.2.2",`
2. `src-tauri/tauri.conf.json` 第 4 行：`  "version": "0.2.1",` → `  "version": "0.2.2",`
3. `src-tauri/Cargo.toml` 第 3 行：`version = "0.2.1"` → `version = "0.2.2"`
4. `src-tauri/Cargo.lock` 第 1573 行：`[[package]]` 块中 `name = "gal-lib"` 紧随其下的 `version = "0.2.1"` → `version = "0.2.2"`。

关键约束：
- Cargo.lock **只改 gal-lib 自身这一个 `[[package]]` 条目的 version**，其余所有依赖条目的 version 一律不动。直接 Edit 第 1573 行即可，无需运行 `cargo update`（仅 workspace 自身包版本变化，不影响依赖解析，也无需联网）。
- 不要执行 `git tag`、`git push` 或任何发布动作 —— 这些由 orchestrator 在本计划之后处理。
  </action>
  <verify>
    <automated>git grep -c '0\.2\.2' -- package.json src-tauri/tauri.conf.json src-tauri/Cargo.toml; git grep -n 'version = "0.2.2"' -- src-tauri/Cargo.lock</automated>
  </verify>
  <done>
package.json / tauri.conf.json / Cargo.toml 三个文件各命中 1 处 `0.2.2` 版本字段（共 3 处）；Cargo.lock 第 1573 行为 `version = "0.2.2"`；`git grep -n '0\.2\.1' -- package.json src-tauri/tauri.conf.json src-tauri/Cargo.toml` 无输出。
  </done>
</task>

</tasks>

<verification>
- `git grep -n '0\.2\.2' -- package.json src-tauri/tauri.conf.json src-tauri/Cargo.toml` 命中 3 处版本字段。
- `src-tauri/Cargo.lock` 中 `name = "gal-lib"` 包条目的 version 为 `0.2.2`，其余依赖条目未改动。
- `git diff --stat` 显示恰好 4 个文件被修改，每个文件仅一行变更。
</verification>

<success_criteria>
四处版本号统一为 0.2.2，组成一个原子提交，提交信息形如 `chore(quick-260517-sm9): bump version to 0.2.2`。未触碰任何其他文件，未执行打 tag / 推送动作。
</success_criteria>

<output>
After completion, create `.planning/quick/260517-sm9-v0-2-2/260517-sm9-SUMMARY.md`
</output>
