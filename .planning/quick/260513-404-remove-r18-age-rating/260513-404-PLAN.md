---
phase: 260513-404
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/migrations/0010_drop_age_rating.sql
  - src-tauri/src/db.rs
  - src-tauri/src/metadata/types.rs
  - src-tauri/src/metadata/bangumi.rs
  - src-tauri/src/metadata/vndb.rs
  - src-tauri/src/ingest.rs
  - src-tauri/src/commands.rs
  - src-tauri/src/lib.rs
  - src/lib/games.ts
  - src/lib/advancedFilter.ts
  - src/lib/search.ts
  - src/components/library/FilterPanel.tsx
  - src/components/library/GameCard.tsx
  - src/routes/Detail.tsx
  - src/routes/Library.tsx
autonomous: false
requirements: []
tags: [quick, refactor, schema-migration, sqlite, tauri, react]

must_haves:
  truths:
    - "新建空 DB 从 0001→0010 顺序迁移成功，games 表不含 age_rating 列"
    - "已存在 v9 DB 应用 0010 后，games 表 age_rating 列被删除，schema_version=10"
    - "cargo check + cargo test + pnpm tsc --noEmit 三绿"
    - "应用启动后 Library/Detail/FilterPanel 都不再出现「R18」「全年龄」字眼"
    - "前端 src/ + 后端 src-tauri/src/ 下 grep 'age_rating|is_r18|R18|全年龄' 仅在 db.rs 测试断言中保留（migration 文件本身允许）"
    - "ingest / bind_metadata / refresh_metadata / refresh_metadata_smart 四条入库路径都不再读写 age_rating，且 SQL UPDATE 语法正确（逗号/COALESCE 完整）"
  artifacts:
    - path: "src-tauri/migrations/0010_drop_age_rating.sql"
      provides: "DROP COLUMN age_rating + schema_version bump to 10"
      contains: "schema_version"
    - path: "src-tauri/src/db.rs"
      provides: "V10 migration 注册 + 单元测试"
      contains: "V10_SQL"
    - path: "src/components/library/FilterPanel.tsx"
      provides: "FilterPanel 不再含 AGE_RATING_OPTIONS 与对应 Section"
      contains: "FilterPanel"
  key_links:
    - from: "src-tauri/src/db.rs"
      to: "migrations/0010_drop_age_rating.sql"
      via: "include_str! + Migration { version: 10, ... }"
      pattern: "0010_drop_age_rating"
    - from: "src-tauri/src/commands.rs apply_ingest_result/bind_metadata/refresh_metadata/refresh_metadata_smart"
      to: "games 表 UPDATE 语句"
      via: "不再 SET age_rating；保持 COALESCE 行的逗号位置正确"
      pattern: "UPDATE games SET"
    - from: "src/routes/Detail.tsx"
      to: "DropdownMenu 菜单结构"
      via: "移除 onSetAgeRating + DropdownMenuSub「R18/全年龄/清除」整块；保留同级菜单项的分隔/缩进"
      pattern: "DropdownMenu"
---

<objective>
彻底删除「R18 / 全年龄」分类的全栈实现 —— 自动判定（Bangumi nsfw / VNDB ero tag）+ DB 列 + IPC + Game 字段 + UI badge + Detail dropdown + FilterPanel facet 全部清除。

Purpose: 当前自动判定数据质量差（VNDB ero tag 阈值偏严 + Bangumi 匿名访问 NSFW 404），用户决定彻底去掉这个分类而不是修复判定。删除是更好的产品决策（少一个错误的 facet 比多一个错误的 facet 强）。

Output:
- 1 个新 migration（0010_drop_age_rating.sql）+ db.rs 单元测试
- 14 个文件改动（7 后端 Rust + 7 前端 TS/React）
- 1 个人工 smoke checkpoint
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# 原始引入此功能的 quick task (历史记录，仅供回滚参考，不动)
# .planning/quick/20260510b-r18-marker-and-custom-views/ — schema v8 同包了 custom_views
# 本次只删 age_rating 列，不动 custom_views / custom_view_games 表

# 关键文件锚点（行号为生成 PLAN 时的快照，执行时以实际为准）
@src-tauri/migrations/0008_add_age_rating_and_custom_views.sql  # 反向 migration 参照
@src-tauri/migrations/0009_add_scan_review_queue.sql            # 最新 migration，下一个为 0010
@src-tauri/src/db.rs                                            # v8/v9 测试模板
@src-tauri/src/metadata/bangumi.rs                              # line 127-129 + 333 nsfw
@src-tauri/src/metadata/vndb.rs                                 # line 148-162 ero-tag 派生
@src-tauri/src/metadata/types.rs                                # MetadataDetail.is_r18
@src-tauri/src/ingest.rs                                        # IngestResult.is_r18 + fetch_enrichment 5-tuple
@src-tauri/src/commands.rs                                      # 5 处 UPDATE + Filter.age_ratings + Game.age_rating + update_game_age_rating
@src-tauri/src/lib.rs                                           # line 231 命令注册
@src/lib/games.ts
@src/lib/advancedFilter.ts
@src/lib/search.ts
@src/components/library/FilterPanel.tsx                         # AGE_RATING_OPTIONS line 72-75
@src/components/library/GameCard.tsx                            # badge line 352-369
@src/routes/Detail.tsx                                          # onSetAgeRating line 640-655 + Sub line 1077-1093
@src/routes/Library.tsx

<sqlite_capability>
项目使用 sqlx 0.8（见 src-tauri/Cargo.toml）— 该版本捆绑的 SQLite ≥ 3.42，原生支持 `ALTER TABLE ... DROP COLUMN`（SQLite 3.35+ 引入）。优先采用单条 DROP COLUMN 语句。

**保险方案（仅在 DROP COLUMN 失败时回退使用）**：CREATE TABLE games_new (无 age_rating 列) → INSERT INTO games_new SELECT (所有列除 age_rating) FROM games → DROP TABLE games → ALTER TABLE games_new RENAME TO games → 重建 games 表上的索引/触发器。如需走此路径，执行前必须用 `PRAGMA table_info(games)` 确认完整列集合并保持 FK/索引等价。
</sqlite_capability>

<interfaces>
<!-- 关键类型与契约。执行时直接遵循，无需再做代码考古。-->

后端 IngestResult（src-tauri/src/ingest.rs）— 删 is_r18 后形态：
```rust
pub struct IngestResult {
    pub title: Option<String>,
    pub cover_url: Option<String>,
    pub metadata_source: Option<String>,
    pub match_confidence: Option<i32>,
    pub brand: Option<String>,
    pub release_year: Option<i32>,
    pub summary: Option<String>,
    pub staff: Vec<StaffEntry>,
    pub official_tags: Vec<String>,
    // 已删: pub is_r18: Option<bool>,
}
```

fetch_enrichment 返回元组：5-tuple → 4-tuple
```rust
// 删除前: (Option<String>, Option<i32>, Option<String>, Vec<StaffEntry>, Option<bool>)
// 删除后: (Option<String>, Option<i32>, Option<String>, Vec<StaffEntry>)
// 调用方 process_game / process_game_cached 的解构同步去掉最后一个 slot
```

MetadataDetail（src-tauri/src/metadata/types.rs）— 删 is_r18 后形态：
```rust
pub struct MetadataDetail {
    pub source: &'static str,
    pub id: String,
    pub title_canonical: Option<String>,
    pub cover_url: Option<String>,
    pub brand: Option<String>,
    pub release_year: Option<i32>,
    pub summary: Option<String>,
    pub staff: Vec<StaffEntry>,
    pub official_tags: Vec<String>,
    // 已删: pub is_r18: Option<bool>,
}
```

前端 Game type（src/lib/games.ts）— 删 age_rating 后形态：
```typescript
export type Game = {
  id: number;
  title: string;
  exe_path: string;
  // ... 其他字段保留
  // 已删: age_rating?: 'all_ages' | 'r18' | null;
};
```

advancedFilter Filter slice — 删 age_ratings 后形态：
```typescript
export type Filter = {
  // ... 其他 facet 保留: brands / years / tags / staff / favorites_only / has_screenshots / custom_view_id ...
  // 已删: age_ratings?: Array<'all_ages' | 'r18'>;
};
// isFilterEmpty 同步删除 age_ratings 的 length 检查
```

Migration 0010 SQL（首选方案）：
```sql
-- 0010_drop_age_rating.sql
-- gal-lib schema v10 — Quick 260513-404
-- Drops games.age_rating column added by 0008 (R18 分类整体删除)
-- 关联代码改动: types.rs / bangumi.rs / vndb.rs / ingest.rs / commands.rs / lib.rs 全栈清理
-- 不动 custom_views / custom_view_games 表（继续保留）
-- Bumps schema_version to 10.

ALTER TABLE games DROP COLUMN age_rating;

UPDATE app_meta SET value = '10' WHERE key = 'schema_version';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 后端 Rust + Migration 0010 全栈清理</name>
  <files>
    src-tauri/migrations/0010_drop_age_rating.sql,
    src-tauri/src/db.rs,
    src-tauri/src/metadata/types.rs,
    src-tauri/src/metadata/bangumi.rs,
    src-tauri/src/metadata/vndb.rs,
    src-tauri/src/ingest.rs,
    src-tauri/src/commands.rs,
    src-tauri/src/lib.rs
  </files>
  <action>
**子步骤 1.1 — 创建 migration 0010**

新建 `src-tauri/migrations/0010_drop_age_rating.sql`（内容见 context.interfaces.Migration 0010 SQL）。

**子步骤 1.2 — 注册 migration + 单元测试**

修改 `src-tauri/src/db.rs`：
- 在 `V9_SQL` 下新增 `const V10_SQL: &str = include_str!("../migrations/0010_drop_age_rating.sql");`
- 在 `migrations()` 向 vec 追加 `Migration { version: 10, description: "drop_age_rating", sql: V10_SQL, kind: MigrationKind::Up }`
- 头部 doc-comment 加一条 "Schema v10 (Quick 260513-404) drops `games.age_rating` column ..."
- 测试模块仿 `migrations_v9_adds_scan_review_queue` 风格新增 `migrations_v10_drops_age_rating`：断言 description、`m10.sql.contains("DROP COLUMN age_rating")`、`add_column_count == 0`、`m10.sql.contains("schema_version") && m10.sql.contains("'10'")`。

**子步骤 1.3 — metadata 层清理 is_r18**

`src-tauri/src/metadata/types.rs`：
- 删 `MetadataDetail` struct 中 `pub is_r18: Option<bool>` 字段定义与相邻 doc-comment

`src-tauri/src/metadata/bangumi.rs`：
- 删 line 127-129 附近 MetadataDetail 构造中的 `is_r18: raw.nsfw` 字段
- 删 SubjectDetail struct（line 330-333）中 `nsfw: Option<bool>` 字段定义
- 如有 #[serde(rename = "nsfw")] / 注释，一并清理

`src-tauri/src/metadata/vndb.rs`：
- 删 line 148-162 整段 ero tag 派生逻辑（变量名通常是 `is_r18` 或 `ero_rating_max`）
- 删 line 184 附近 MetadataDetail 构造中的 `is_r18` 字段写入
- 若 VndbTag 结构体里有 ero rating 相关字段，同步检查是否还有人用；无用则删（保持 minimal diff，不影响其他 tag 逻辑）

**子步骤 1.4 — ingest 层清理**

`src-tauri/src/ingest.rs`：
- 删 `IngestResult` struct 的 `pub is_r18: Option<bool>` 字段
- `fetch_enrichment` 函数返回元组从 5-tuple 改 4-tuple，删 is_r18 slot 与函数体内对 detail.is_r18 的读取/累积逻辑
- 5 处 IngestResult 构造（grep `IngestResult {`）去掉 `is_r18` 字段：process_game / process_game_cached / refresh_for_query placeholder / fetch_enrichment 自身 / 其他遗漏点
- process_game / process_game_cached 中对 `fetch_enrichment` 返回值的解构同步去掉最后一个变量绑定

**子步骤 1.5 — commands.rs 清理（5 处 UPDATE + Filter + Game row mapping + 函数 + SELECT 列表）**

`src-tauri/src/commands.rs`：

(a) **apply_ingest_result**（约 line 176-189）：删 `age_rating_str` 派生（从 is_r18 推导 'r18'/'all_ages'/None 的整段）+ UPDATE SET 子句里的 `age_rating = COALESCE(?, age_rating)` 行 + 对应的 bind。**特别注意保留前后行的逗号正确。**

(b) **bind_metadata UPDATE**（约 line 1021/1031/1045）：SET 子句删 `age_rating = COALESCE(?, age_rating)` 与 bind。

(c) **refresh_metadata UPDATE**（约 line 1127/1138/1153）：同上。

(d) **refresh_metadata_smart 未绑定 + 已绑定两条 UPDATE**（约 line 1313/1325/1340 与 1465/1487/1495）：同上，两处都要清。

(e) **update_game_age_rating 函数**（约 line 2711-2740）：整个 `pub async fn update_game_age_rating` 删除（含 `#[tauri::command]` 属性、入参签名、SQL UPDATE、doc-comment）。

(f) **Filter struct**（约 line 2058）：删字段 `pub age_ratings: Option<Vec<String>>`。

(g) **Filter SQL builder**（约 line 2209-2244）：删处理 `filter.age_ratings` 的整段分支（IN 子句拼接 + push_bind 循环）。注意：上下相邻分支的 `if let Some(...)` / `where_clauses.push(...)` 衔接必须保持语法正确。

(h) **Game struct row mapping**（约 line 1620 doc-comment + line 1683 `row.try_get::<Option<String>, _>("age_rating").ok().flatten()`）：删字段与 row mapping。

(i) **SELECT 列表**（grep `age_rating,` 与 `, age_rating ` 在 commands.rs 全文）：清掉每一处 SELECT 列。常见位点 line 1639 / 2060 / 2270 / 3293 / 3505 —— **以实际 grep 为准**。注意 SELECT 列表里删字段后逗号位置仍正确（最后一列不能有尾随逗号）。

**子步骤 1.6 — lib.rs 取消命令注册**

`src-tauri/src/lib.rs` line 231：删除 `commands::update_game_age_rating,` 这一行（注意保留前后行命令注册的逗号正确）。

**子步骤 1.7 — 验证清理彻底**

执行以下 grep（不应有任何业务代码命中，只允许 db.rs 测试断言 + migration 文件本身命中）：
```
grep -rnE "age_rating|is_r18|nsfw|all_ages|R18|全年龄" src-tauri/src/
```

预期：
- src-tauri/src/db.rs 中只剩 migration_v8 / migration_v10 测试中的断言字符串（"age_rating IN ('all_ages','r18')" 字面量 与 "DROP COLUMN age_rating"）—— 这些是历史断言/新断言，**保留**
- 其他 .rs 文件应**零命中**
- migration 文件本身（0008/0010）命中是预期，不动

**注意事项**：
- bangumi.rs SubjectDetail 删 nsfw 字段后，如该字段被 serde 反序列化但未读，编译器不报错也无影响；但若有 `#[serde(default)]` / 其他属性，删字段时一并清掉相应注解
- 如 cargo test 中有引用 IngestResult/MetadataDetail 的 .is_r18 字段，同步删除断言（grep 也会找到）
- COALESCE 行删除后用 `cargo check` 立即验证 SQL 不会有语法错误（虽然 SQL 是 runtime 检查，cargo check 至少保证字符串拼接的 Rust 代码合法）
  </action>
  <verify>
    <automated>cd src-tauri && cargo check --all-targets 2>&1 | tee /tmp/cargo-check.log && cargo test --lib db::tests::migrations_v10_drops_age_rating -- --nocapture 2>&1 | tee -a /tmp/cargo-check.log && cargo test --lib 2>&1 | tail -30</automated>
  </verify>
  <done>
- `src-tauri/migrations/0010_drop_age_rating.sql` 存在且含 `ALTER TABLE games DROP COLUMN age_rating;` 与 schema_version=10 更新
- `src-tauri/src/db.rs` 注册 V10 migration 且 `migrations_v10_drops_age_rating` 测试通过
- `cargo check --all-targets` 零警告零错误
- `cargo test --lib` 全部通过（含 v10 新测试）
- `grep -rnE "age_rating|is_r18|nsfw|all_ages" src-tauri/src/*.rs src-tauri/src/metadata/*.rs` 仅 db.rs 测试断言 与 commands.rs 中**不存在**业务代码命中（migration .sql 不算源码）
- `commands::update_game_age_rating` 函数在源码中不存在；lib.rs 未注册
  </done>
</task>

<task type="auto">
  <name>Task 2: 前端 TS/React 全栈清理</name>
  <files>
    src/lib/games.ts,
    src/lib/advancedFilter.ts,
    src/lib/search.ts,
    src/components/library/FilterPanel.tsx,
    src/components/library/GameCard.tsx,
    src/routes/Detail.tsx,
    src/routes/Library.tsx
  </files>
  <action>
**子步骤 2.1 — types & wrappers**

`src/lib/games.ts`：
- 删 `Game` type 中 `age_rating?: 'all_ages' | 'r18' | null` 字段（含上方注释）
- 删 `updateGameAgeRating(id, value)` 整个 wrapper 函数（含 `invoke('update_game_age_rating', ...)` 调用与 export）
- 如有 `AgeRating` 类型别名 / 常量，一并删除

**子步骤 2.2 — advancedFilter**

`src/lib/advancedFilter.ts`：
- 删 `Filter` type 中 `age_ratings?: ...` 字段
- 删 `isFilterEmpty` 函数中 `(!f.age_ratings || f.age_ratings.length === 0)` 这一项（**注意 20260512g 刚加过此判定，本次顺手清除**）—— 删除后保持函数体逻辑等价于"其他所有 facet 都为空才算 empty"
- 如有 facet count 计算 / 序列化 / URL hash 中带 age_ratings，全部清除

**子步骤 2.3 — search.ts（如有引用）**

`src/lib/search.ts`：
- grep `age_rating|ageRating` 全清。可能在 getSidebarCategories / facet count 累加 / serializer 中
- 如该文件不含 age_rating 引用，跳过

**子步骤 2.4 — FilterPanel**

`src/components/library/FilterPanel.tsx`：
- 删 `AGE_RATING_OPTIONS` 常量（约 line 72-75）
- 删整段「年龄分级」FilterSection / Checkbox group UI（含 onChange handler 与 selected 状态衔接）
- 删 Filter prop 解构里的 `age_ratings`、`onAgeRatingsChange`（若存在）
- 删 facet count 计算中对 age_ratings 的累加

**子步骤 2.5 — GameCard badge**

`src/components/library/GameCard.tsx`（line 340-369）：
- 删 `r18` 分支的 badge JSX（红色 R18 角标）
- 删 `all_ages` 分支的 badge JSX（绿色全年龄角标）
- 删上方注释（如 "// R18/全年龄 visual marker"）
- 删依赖的 helper（如 isR18(game) / ageRatingLabel(game)），如其他地方仍未使用

**子步骤 2.6 — Detail 页 onSetAgeRating + DropdownMenuSub**

`src/routes/Detail.tsx`：
- 删 `onSetAgeRating` async handler（line 640-655）
- 删 `import { updateGameAgeRating } from '@/lib/games'` 这行 import
- 删 DropdownMenu 中「年龄分级」DropdownMenuSub 块（line 1077-1093 + 其 SubTrigger / SubContent / 三个 MenuItem「R18 / 全年龄 / 清除」）
- 确保删除后相邻 DropdownMenuItem / Separator 顺序与缩进自然衔接，不留空 Sub

**子步骤 2.7 — Library 页**

`src/routes/Library.tsx`：
- grep `age_rating|R18|全年龄|all_ages` 全清。可能在 sidebar 计数展示 / quick filter chips / URL state hydration 中
- 如有 advancedFilter 同步逻辑中以 age_ratings 作为 key 解析 URL hash，一并清除

**子步骤 2.8 — 验证清理彻底**

```
grep -rnE "age_rating|is_r18|R18|全年龄|all_ages|updateGameAgeRating|AGE_RATING_OPTIONS|onSetAgeRating" src/
```

预期：**零命中**（前端不像后端有测试断言保留，应该完全干净）。

**注意事项**：
- TypeScript 编译器会通过 `tsc --noEmit` 把所有遗漏（如 stale 引用、类型不匹配）报出来；优先信赖 tsc 报错而不是手工 grep
- 删 Detail.tsx 中 import 后注意是否还有其他从 '@/lib/games' import 的成员；不要误删整行
- FilterPanel 删 Section 后，如父组件用 grid/flex 布局，可能需要调整 gap；但本次目标是删除，**不做样式重排**，让自然 spacing 处理
  </action>
  <verify>
    <automated>cd /d/project/gal-lib && pnpm tsc --noEmit 2>&1 | tee /tmp/tsc.log && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
- `pnpm tsc --noEmit` 零错误
- `pnpm build` 成功（vite + tauri build dev manifest 全绿）
- `grep -rnE "age_rating|is_r18|R18|全年龄|all_ages|updateGameAgeRating" src/` 完全为空
- `src/components/library/FilterPanel.tsx` 不含 `AGE_RATING_OPTIONS`；视觉上无「年龄分级」Section
- `src/routes/Detail.tsx` 不含 `onSetAgeRating`；DropdownMenu 中无「R18/全年龄/清除」子菜单
- `src/lib/games.ts` 不含 `updateGameAgeRating` export；`Game` type 无 `age_rating` 字段
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 人工 smoke — DB + UI 验证彻底删除</name>
  <what-built>
- DB schema v10：games.age_rating 列被 DROP；schema_version=10
- 后端：metadata/ingest/commands 全栈清除 is_r18 / age_rating；update_game_age_rating IPC 注销
- 前端：FilterPanel 无年龄分级 facet；GameCard 无 R18/全年龄 badge；Detail DropdownMenu 无相关子菜单
  </what-built>
  <how-to-verify>
**前置**：备份当前 data/gal-lib.db（user_data 目录下），便于回滚。

**1) 启动新版应用（pnpm tauri dev 或 release build）。**

**2) 验证 DB schema：**
- 打开 SQLite CLI 或 DB Browser 连接 data/gal-lib.db
- 运行 `PRAGMA table_info(games);` → 输出列名中**不应**出现 `age_rating`
- 运行 `SELECT value FROM app_meta WHERE key='schema_version';` → 应返回 `10`
- 运行 `SELECT name FROM sqlite_master WHERE type='table';` → 应包含 `custom_views` / `custom_view_games`（不动）

**3) UI 验证（Library 页）：**
- 左侧 FilterPanel 滚动一遍：**不应**看到「年龄分级 / Age Rating / R18 / 全年龄」字眼
- 游戏卡片网格：**不应**看到任何卡片角落的 R18（红色）或「全年龄」（绿色）badge
- 在搜索框输入「R18」「全年龄」分别尝试：应为常规文本搜索，不应有专门的 facet 过滤入口

**4) UI 验证（Detail 页）：**
- 点开任意游戏的详情页
- 右上角「更多」DropdownMenu 展开：**不应**看到「年龄分级」子菜单（也即不应有 SubTrigger 上挂着「R18 / 全年龄 / 清除」三项的入口）
- 其他菜单项（如品牌、发行年份、删除等）应保持完好

**5) 全量扫描 smoke（可选但推荐）：**
- Settings 页点「全量扫描」（或「刷新元数据」）
- 等待完成，确认无报错（特别是不应有 SQL "no such column: age_rating" 错误）

**6) 控制台日志检查：**
- DevTools Console + Tauri terminal log 中不应有 `update_game_age_rating` 相关的「command not found」错误（若有，说明前端某处仍在调用，需要回到 Task 2 补查）

**预期结果**：上述 6 项全部通过；DB 表结构精简；UI 中 R18 概念完全消失；扫描 / 刷新流程未被破坏。

**若失败**：记录失败点 → 反馈具体现象（哪个步骤、什么报错） → 让 Claude 进入修复迭代。
  </how-to-verify>
  <resume-signal>输入 "approved" / "ok" / "通过" 继续收尾（commit + SUMMARY），或描述发现的问题</resume-signal>
</task>

</tasks>

<verification>
**自动化（Task 1+2 之后）：**
- `cd src-tauri && cargo check --all-targets` 零警告零错误
- `cd src-tauri && cargo test --lib` 全部通过（含新增的 `migrations_v10_drops_age_rating`）
- `pnpm tsc --noEmit` 零错误
- `pnpm build` 成功

**Grep 不变量（Task 1+2 之后）：**
```
grep -rnE "age_rating|is_r18|R18|全年龄|all_ages|updateGameAgeRating|AGE_RATING_OPTIONS|onSetAgeRating" src/ src-tauri/src/
```
预期：仅 `src-tauri/src/db.rs` 中的 v8/v10 测试断言字符串 + `src-tauri/migrations/0008_*.sql` / `0010_*.sql` 文件本身命中。其他**零**。

**人工（Task 3 checkpoint）：**
- DB pragma 验证 games 表无 age_rating 列；schema_version=10
- Library/Detail/FilterPanel 三处 UI 无 R18/全年龄字眼
- 扫描 / 刷新流程无 SQL 报错
- DevTools 无 update_game_age_rating 相关 IPC 错误
</verification>

<success_criteria>
- 1 个新 migration 文件 + db.rs 单元测试到位
- 14 个文件改动全部完成（7 后端 + 7 前端）
- 后端三绿：cargo check + cargo test + 无业务 grep 命中
- 前端三绿：tsc --noEmit + build + 无业务 grep 命中
- 人工 smoke：DB schema 正确 + UI 干净 + 扫描可用
- SUMMARY.md 写明回滚提示（参考 20260510b 原实现 + 写 0011 migration ADD COLUMN back）
</success_criteria>

<output>
After completion, create `.planning/quick/260513-404-remove-r18-age-rating/260513-404-SUMMARY.md` capturing:
- 改动文件清单（14 + 1 migration = 15）
- 验证结果（cargo + tsc + 人工 smoke 三段）
- 回滚提示：如需恢复 R18 功能，参考 `.planning/quick/20260510b-r18-marker-and-custom-views/` 原实现 + 新建 0011_re_add_age_rating.sql（`ALTER TABLE games ADD COLUMN age_rating TEXT CHECK(...);` + bump schema_version=11）
- 已知遗失数据：`metadata_source='manual'` 的游戏先前手动覆盖的 age_rating 值随列删除一并丢失（用户已确认）
- Commit hash（执行完成后回填）
</output>
